# HopeTSIT — Memory index

- [v498 renommage package + crash GetX](v498-package-rename.md) — package Android → com.cardellihermanos.hopetsit (Google Play) ; crash GetX déjà corrigé (vieux builds) ; 2 actions console Daniel (Firebase + Maps key).
- [Correctif Apple Sign-In + compte démo store](apple-signin-fix-and-demo-account.md) — fix iOS Apple (3 causes, Mac local) + RÈGLE : compte démo store DOIT être email+mot de passe (OAuth Google/Apple = pas de mdp → refus Apple ET Google).
- [Couronne premium = 4+ sources](couronne-premium-multi-sources.md) — isPremium calculé à 4 endroits (fetchUserMini, members/nearby, live-positions, web premiumIds) ; tous doivent compter staff+cross-rôle ; PawSpot ≠ couronne ; vieux comptes sans couronne = données email/oldId non liées.

- **RÈGLE Daniel (v460+)** : à CHAQUE build APK, copier l'APK dans `Downloads/HoPetSit_v23.1.<ver>.apk` systématiquement (sans qu'il le redemande).
- **VÉRIF SYNC app↔web + promo (PUSH 4a4c092, web-only)** — Daniel : « vérifie synchro signalements/pawspot/pawpoints + code promo câblé partout ». RÉSULTAT (3 agents Explore) : (1) **Signalements + PawSpot = SYNCHRO** — app et web tapent les MÊMES routes backend (`/map-reports/*`, `/pawspots/*`), mêmes modèles, mêmes params (seul écart mineur : web n'envoie pas `photoUrl` à la création = pas de photo côté web, pas un pb de synchro). (2) **PawPoints = SYNCHRO** — app+web lisent `/pawpoints/catalog` + `/pawpoints/me` ; niveaux/bonus calculés SERVEUR (pas client) → lifetime/spendable/level identiques. (3) **Code promo** = câblé backend (`/promo/check`+`/promo/redeem`) + admin (onglet Promotions) + app (3 profils) + web (BOUTIQUE déjà). AJOUT : carte « Code promo » aussi sur la page PROFIL web (`profile/page.tsx`, réutilise `redeemPromo`, clés i18n promo_* existantes) pour parité avec l'app. tsc clean.
- **WEB page Supprimer mon compte (PUSH f25a6e2, web-only, exigence Google Play)** — page publique **`/delete-account`** (= URL à coller dans Play Console « Suppression du compte »). Explique RGPD (profil/animaux/annonces/messages supprimés, retiré amis/familles/map, factures anonymisées conservées par loi) ; **connecté** → case confirmation + bouton rouge → `deleteMyAccount()` = `DELETE /users/me` (`deleteAccountFromToken`, cascade RGPD déjà existante) → `clearAuth` + redirige ; **non connecté** → CTA `/login?redirect=/delete-account` + note « aussi possible dans l'app ». Lien dans footer section Légal/Confidentialité. `api.ts` +`deleteMyAccount`. i18n +16 clés ×6 (`del_*` + `footer_delete_account`). ⚠️ login web lit `?redirect=` (pas `?next=`). tsc clean. PAS de rebuild app.
- **WEB facture « Server error » (vraie cause) + Google login montrait l'URL Render (PUSH 093c03c, web+backend, PAS de rebuild app)** — (1) **Server error PDF facture** : CAUSE RACINE = `invoiceController.listMyInvoices` renvoyait des docs `.lean()` avec `_id` (pas `id`) → le site lisait `inv.id`=undefined → URL `/invoices/undefined/html` → `findById('undefined')` = CastError → 500. FIX : expose `id: inv._id.toString()` dans listMyInvoices + `renderInvoiceHtml` rejette un id non-ObjectId (regex 24-hex) en 404 au lieu de 500. (Le fix v069bfe5 open robuste + null-safe provider reste, mais ÇA c'était la vraie cause.) (2) **Google login web affichait l'URL Render** : CAUSE = `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` mal réglé (pointait sur le backend Render) → popup OAuth affichait Render. FIX `website/src/lib/firebase.ts` : force le domaine d'auth canonique `<projectId>.firebaseapp.com` si l'env est absente OU ne finit pas par firebaseapp.com/web.app. tsc + node -c clean.
- **WEB Mes factures — bouton Télécharger PDF réparé (PUSH 069bfe5, web+backend, PAS de rebuild app)** — Daniel : « le bouton télécharger PDF marche pas ». 2 causes : (1) `invoices/page.tsx` `openInvoice` faisait `window.open(url,'_blank','noopener,noreferrer')` → le 3e arg (window features) fait que certains navigateurs/bloqueurs de popup IGNORENT l'ouverture (clic « sans effet ») → remplacé par clic `<a target=_blank rel=noopener>` (fiable) + repli `location.href` ; si pas de token → `/login`. (2) Backend `invoiceController.renderInvoiceHtml` : une facture d'abonnement/promo SANS `providerId` → `inv.providerId.toString()` levait TypeError → 500 « Server error » → comparaison via `String(inv.ownerId||'')`/`String(inv.providerId||'')` null-safe. La facture HTML s'ouvre dans un nouvel onglet (bouton interne « Télécharger PDF » = `window.print()` sur le web). tsc + node -c clean.
- **WEB PawMap — refonte barre de contrôles (PUSH 1018bc5, web-only)** — maquette `PawMap-Boutons.html` + `PROMPT-integration.md` (Downloads). Carte INCHANGÉE ; `map/page.tsx` zone contrôles refondue (handlers réutilisés) : (1) barre d'actions Signaler #e5342a / Voir signaux #fbe4e1·#c0352b | Taguer un spot #e83e8c / Voir spots #fce0ef·#c2367f + légende avatar membre ROSE (sans halo) « Utilisateur » ; (2) bandeau « Amis en direct » (pastille verte ping + compteur + puces livePositionsList, toggleFriendsLayer) ; (3) bandeau « Abonnements actifs » (puces dynamiques selon benefits : PawPremium noir/or, PawSpots doré, PawFamily/PawFollow violet + Gérer→/boutique) ; (4) filtre catégorie chip actif vert #1f8a4c. i18n +5 clés ×6 (map_spots_chip/map_live_friends/map_active_subs/map_manage/map_member_legend). Supprimé `LayerChip`+consts couleurs (inutilisés). tsc clean. ⚠️ le .dc.html de réf est un bundler Claude-Design (markup encodé) → on s'est basé sur le PROMPT (couleurs exactes) + conventions Tailwind du site (pas les polices Bricolage/Hanken).
- **Compteur visites spot sur le WEB (PUSH 40a9a43, web-only)** — Daniel : « vérifie que le compteur de visites du spot marche app+web ». VÉRIF : backend `POST /pawspots/:id/visit` incrémente `visitsCount` 1×/user (`visitedBy`) ✓ ; APP appelle `/visit` + affiche 👣 ✓ ; WEB = NE l'appelait PAS + n'affichait pas 👣. FIX web : `api.visitSpot(id)` (POST → renvoie total) ; `PoiMap` spot Marker `eventHandlers.popupopen → onSpotVisit` + popup affiche `· 👣 visitsCount` (à côté ❤️/⭐) ; `map/page.handleSpotVisit` MAJ le compteur local avec le total backend. tsc clean. → app+web synchro sur le compteur.
- **Notifs traduites À LA LECTURE + onglet Code promo dashboard (PUSH a8bb4ee)** — Daniel : « je suis en espagnol mais les notifs du site sortent en FR ». CAUSE = notifs rendues au SEND-time + stockées (titre/corps figés). FIX BACKEND : `notificationController.getMyNotifications` re-rend titre/corps via `notificationSender.renderNotificationContent(type, data, locale)` depuis l'`appLocale||language` du lecteur → les notifs suivent la langue COURANTE (Notification stocke type+data → re-render OK), app+web, fallback texte stocké si pas de template. ⚠️ il faut que l'appLocale du lecteur soit à jour (web sync ajouté avant) → se déconnecter/reconnecter une fois en ES. + WEB dashboard : onglet « Code promo » (SideLink ancre `#promo`) + carte promo dans la zone principale (redeemPromo). tsc + node -c clean.
- **WEB PawMap membres ROSE (PUSH 4fef492, web-only)** — Daniel : « je vois les utilisateurs en rose sur le web ? » → AVANT non (calque app v497 only). AJOUT parité : `api.getNearbyMembers` (`/friends/members/nearby`) + `PoiMap` prop `members` + `makeMemberIcon` (cercle rose dégradé #F06AA0→#E0568B + patte blanche + 👑 si premium + point vert/gris). `map/page.tsx` fetch quand VIEWER abonné (pawspot/premium/isPremium), refetch au pan, même gating que l'app. tsc clean.
- **WEB PawMap 4 boutons + sync langue notifs (PUSH ab73b3d, web-only)** — Daniel : « rajoute les 4 boutons PawMap comme l'app + pack langue notifs manquant ». (1) `/map` : **Voir spots** (existant) + **Voir signaux** (NEW couche reports : `getNearbyReports` + markers emoji `makeReportIcon` dans `PoiMap`, prop `reports`/`reportTypeLabels`) + **Tag spot** + **Signaler** = création au **CENTRE de la carte** via modal (type+note), **GATED abo** PawSpot/Premium (sinon → /boutique ; 402 backend géré). `api.ts` +`createMapReport`/`createPawSpot`/`syncAppLocale`. i18n +17 clés ×6 (chip/boutons/modal + 6 types signalement curados : lost_pet/found_pet/aggressive_dog/dead_animal/stray_pet/water_active + erreurs). (2) **Notifs « pack langue »** : DIAGNOSTIC = catalogue backend `locales/*/notifications.json` COMPLET 42/42 ×6 + bandeau web `t()` OK → pas de clé manquante ; la vraie cause = le SITE ne poussait jamais sa langue au backend (seule l'app le faisait) → notifs/emails dans l'ancienne langue. FIX : `LanguageProvider` appelle `syncAppLocale` (PATCH /users/me/app-locale) au chargement + à chaque changement de langue si connecté. ⚠️ les notifs DÉJÀ envoyées gardent leur langue d'origine (rendues au send-time, comme l'app). tsc clean.
- v497 (PUSH origin **9136611** ; build 23.1.497+497 → `Downloads/HoPetSit_v23.1.497.apk`) — **icône rose « membres PawMap proches » élargie TOUS RÔLES** (Daniel : 2 comptes même lieu, viewer Gardien premium voyait rien car la fonction = owner-voit-providers). Choix Daniel : « tous rôles voient tous les membres PREMIUM ». **Backend** : nouvel endpoint `GET /friends/members/nearby?lat&lng&radiusInMeters` (friendRoutes, AVANT routes /:id) = geo `$near` sur Owner+Sitter+Walker (Owner a déjà 2dsphere), filtre membres ABONNÉS actifs (mapBoostExpiry/PawSpot OU UserSubscription premiumExpiry/currentPeriodEnd/familyExpiry OU isStaff), exclut soi-même (id/email/oldId sur 3 rôles), renvoie {id,role,name,avatar,location,isPremium,isPawSpot,isOnline}. **App** `paw_map_screen` : `_loadNearbyProviders` repointé sur `/friends/members/nearby` (au lieu de /sitters+/walkers/nearby) ; appelé pour TOUS les rôles dans `_reloadAtCenter` (avant owner-only) ; `nearbyVisible` (markers) + halo rose : drop `!_isSitterOrWalker` → gardés sur abo VIEWER (pawspot/premium) + `_showProviders`. Confidentialité = seuls les membres avec abo actif apparaissent. node -c + flutter analyze clean. ⚠️ pour tester : les DEUX comptes doivent avoir un abo actif (PawSpot/Premium) + une position, à <25km.
- **FIX premium STAFF cross-role (PUSH 97bcecb, backend-only sans rebuild APK)** — Daniel : « ami mis staff mais tjr pas premium ». CAUSE = `isStaff` posé sur 1 SEUL doc rôle ; lecture (`/me/benefits` + `/subscriptions/status`) ne regardait QUE le doc du rôle courant. FIX : fallback — si `isStaff` false sur le doc courant, relire sur les 3 docs (`$or` email/oldId, `isStaff:true`) → premium garanti quel que soit le rôle, **SANS re-cocher l'admin ni réinstaller l'app** (l'ami se déconnecte/reconnecte). La couronne vue par les AUTRES (`friendMini`) lit encore le doc de l'amitié → re-cocher staff admin (propagation v495 sur 3 docs) pour qu'elle s'affiche. ⚠️ l'APK ne rend PAS premium (logique déjà là depuis v426) — c'était un pb de DONNÉE (quel doc a isStaff).
- v496 (PUSH origin **dedf845** ; build 23.1.496+496 → `Downloads/HoPetSit_v23.1.496.apk`) — **email/deep-link/web branding/PawFollow**. (1) **GROS BUG écran NOIR depuis email** : bouton « Voir la demande » → App Link ouvre l'app → `deep_link_service` faisait `Get.to(FriendsScreen)` MÊME sans session → écran NOIR (controllers/auth absents). FIX : garde `_hasSession()` (Get.find<ApiClient>().authToken) — si pas de session ET lien non-public (≠ open/app/auth) → no-op gracieux (onboarding/login s'affiche, plus d'écran noir). + `emailLinkBuilder` : friend/friend_request/live_tracking renvoient désormais `APP_OPEN_LINK` (`/open`) au lieu de `/friends/live` → web Chrome redirige vers `/download`, app connectée s'ouvre (demande déjà dans bandeau+cloche). (2) **Nom expéditeur email** : `emailService.BRAND_NAME` lisait `process.env.EMAIL_BRAND_NAME` (mal réglé « HopeTSIT » sur Render) → FORCÉ en dur `'HoPetSit'`. (3) **WEB rename** : « Paw Premium »→« PawPremium » + « Paw Map »→« PawMap » sur TOUT le site (sed `src/**/*.{ts,tsx}` ; 104+29 occ.) + les 2 SVG logos `<text>Paw Map`→`PawMap`. ⚠️ occ. dans api.ts/PoiMap/PawMemberBadge = COMMENTAIRES (sans risque). PAS touché `public/HoPetSit.apk` (binaire). (4) **Desc web** : `pawspot_desc` +phrase « voir les utilisateurs PawMap proches (icône rose 🌸) » ×6 ; `pawfollow_desc` +phrase « bouton suivre en direct on/off — tu me suis, je te suis » ×6 (script Python). (5) **App PawFollow tab** : nouvelle ligne feature `pawfollow_feature_live_toggle` (icône share_location) dans `coin_shop_screen._buildFeaturesList` + clé ×6 langues. tsc web OK + node -c + flutter analyze clean. **(6) FOLLOW-UP web accueil (PUSH 8d99a54, web-only sans rebuild APK)** : grille 3 services `page.tsx` → PawFollow + PawSpot **n'affichent plus de prix** → CTA « Découvrir <marque> » pleine largeur (PetSitting garde son prix marketplace) ; bande PawPremium → titre + 👑 **alignés horizontalement** (flex items-center) + CTA « Découvrir PawPremium ». Nouvelle clé i18n `home_discover` (verbe) ×6.
- v495 (PUSH origin **6c91866** ; build 23.1.495+495 → `Downloads/HoPetSit_v23.1.495.apk`) — **DEEP amis/notifs/premium-staff/langue**. (1) **Notifs+emails en FR pour ami espagnol** : `notificationSender` priorise déjà `appLocale||language` MAIS `appLocale` n'était synchro qu'au chargement accueil (`home_quick_action_bar`). FIX : `auth_controller` appelle `LocalizationService.syncToBackend()` DÈS le login (3 paths, après `_registerFcmTokenWithBackend`) → langue UI (choisie OU device) poussée immédiatement (guard `_syncedThisSession`). (2) **Badge/couronne premium STAFF absent** : CAUSE RACINE = `isStaff` posé sur 1 SEUL doc rôle (admin `/users/:role/:id/staff`). 1 compte = 3 docs (Owner/Sitter/Walker) → si l'ami se connecte sous un autre rôle = isStaff false. FIX `adminRoutes` : propage isStaff sur les 3 docs via `$or:[{_id},{email},{oldId}]` (comme gatherFcmTokens). (3) `friendRoutes.fetchUserMini` : `isPremium` comptait QUE l'abo payant (`premiumExpiry`) → staff sans abo = pas de couronne. FIX : `select +isStaff` + `isPremium=true si isStaff` (couronne map + badge carte ami) + `hasPawFollow=true si isStaff` (apparaît dans « En direct »). `/me/benefits` comptait DÉJÀ isStaff (v426, profil perso OK). (4) **VÉRIFIÉ câblage demande d'ami** : route POST /friends/request crée Friendship + `sendNotification('friend_request_received')` → in-app (createNotificationSafe) + push + email, template présent dans les 6 locales ; bandeau accueil 3 rôles via `home_quick_action_bar._pickFriendRequestAction` (lit `FriendController.incomingRequests`, partagé owner/sitter/walker). TOUT est câblé → si l'ami ne reçoit pas = il est sur un APK ANCIEN (lui dire d'installer v495). node -c + flutter analyze clean.
- v494 (PUSH origin **1028f5c** ; build 23.1.494+494 → `Downloads/HoPetSit_v23.1.494.apk`) — 3 réglages. (1) **PawMap petite carte** : `_roundMapBtn` 44→38px+icône 20→18, `_buildMapActionsColumn` gaps 12→8.h, colonne **ANCRÉE EN HAUT** `Positioned(left:12,top:8, _buildMapActionsColumn())` (avant centrée `top:0,bottom:96+inset,Center`) → « Signaler » (4e) plus masqué par le menu. (2) **Rayon bulle km MARRON→ROSE** : `around_me_search_bar` SliderTheme `valueIndicatorColor:#EC4899`+`valueIndicatorTextStyle` blanc w700 (3 profils + tous onglets). (3) **Inscription p.2 bouton auto-détection re-ajouté** : `signup_wizard._stepProviderLocation` OutlinedButton.icon « Détecter ma position » (`signup_location_auto`/`location_getting`, `c.getCurrentLocationFromMaps`, accent rôle) AU-DESSUS du CityLocationPicker (3 rôles). flutter analyze clean.
- v492 (PUSH origin **ab285dc** ; build 23.1.492+492 → `Downloads/HoPetSit_v23.1.492.apk`) — finitions inscription + nav. **(1) Ville AUTOCOMPLETE inscription** : `signup_wizard_screen._stepProviderLocation` (sitter/walker) — remplacé `_field(cityController)` + bouton « ma position » par `CityLocationPicker` (widget existant : autocomplete Nominatim, tape « Paris » → suggestions → tap → `onLocationSelected(city,lat,lng)` remplit `userCity/userLatitude/userLongitude`). Import `widgets/city_location_picker.dart`. Owner : pas de champ ville à l'inscription (auto-détecté) mais a déjà CityLocationPicker en édition profil + home + publish. **(2) Photo profil inscription → PROFIL (3 rôles)** : la photo était uploadée en fire-and-forget après OTP + profil caché sans avatar → invisible. FIX `otp_verification_controller` : `await _uploadPendingSignupPhotoIfAny()` (au lieu de fire-and-forget) + après upload, écrit `result['avatar']['url']` dans `user_profile` (GetStorage) → en-tête profil l'affiche direct. Backend `updateProfilePicture` gère déjà owner/sitter/walker (Model par rôle). **(3) Photo MISE EN ÉVIDENCE étape 1** (Daniel : « tout le monde loupe l'ajout ») : bloc carte teintée `_accent.08` + bord accent + Row [Column « Photo de profil » (`signup_photo_label`) + indice (`signup_photo_hint`) À GAUCHE] [cercle photo À DROITE : anneau accent 2.5 + halo boxShadow + icône `add_a_photo_rounded`]. i18n +2 clés ×6. **(4) Bouton central nav** : `'Paw Map'` → `'PawMap'` (collé) dans `stacked_navigation_wrapper.dart` + `custom_navigation_bar.dart`. **Pack langue vérifié** : 0 clé FR manquante dans les 6 langues (de/it/pt ont ~26-33 clés EN PLUS, orphelines, sans impact). flutter analyze clean. ⏭️ PROCHAINE ÉTAPE = refonte DESIGN du SITE WEB (3 pages : Accueil/PawMap/Dashboard) design-only, maquettes extraites dans `/c/tmp/web_mockups/` (HoPetSit Accueil/Dashboard/PawMap .dc.html + PROMPT-refonte-design.md).
- v491 (PUSH origin **7293ec3** ; build 23.1.491+491 → `Downloads/HoPetSit_v23.1.491.apk`) — retours Daniel post-v490. **(1) PawMap 4 boutons** (Voir spots/Tag spot/Voir signaux/Signaler) : le 4e (Signaler) coupé par le menu → `Positioned(left:12, top:0, bottom: 96.h + viewPadding.bottom, Center(child:_buildMapActionsColumn))` (avant bottom:0). **(2) Bandeaux Valider** spot+signalement remontés `(pawMapExpanded ? 64 : 116)` (avant 48/96) — 2 occurrences (indentations différentes, edit séparé). **(3) Inscription : design ARRONDI RE-APPLIQUÉ en version SÛRE** après le revert v490 (Daniel : « pkoi t'es revenu à l'ancien »). Nouveau helper `_roundedDec({label,hint,suffixText})` = bordure 14r + focus `_accent`, **SANS `filled`/`context`/`Builder`/`Obx(()=>Builder)`** (le thème gère dark-mode). `_radiusDropdown` = `Obx(() => DropdownButtonFormField(...))` lit `.value` DIRECT dans l'Obx (pas de Builder). `_PasswordField` bordures arrondies inline (widget.accent). country picker radius 14. **(4) Utilisateurs SUPPRIMÉS retirés** : `listConversations` (conversationController) → si l'autre participant n'existe plus (otherDoc null après populate Owner/Sitter/Walker), `return null` + `.filter(Boolean)` AVANT la réponse → plus de ghost gris « Utilisateur supprimé » (avant : il fabriquait cet objet). `/sitters/nearby` : ajout `status: { $nin: ['suspended','banned'] }` au filter geoNear (walker déjà filtré via baseStatusFilter). **(5) Boutique logo rose** : fonction TOP-LEVEL `_pawBadgeMini()` (cercle rose dégradé #F06AA0→#E0568B + patte blanche, ombre) — ⚠️ TOP-LEVEL car onglets PawSpot (`_PawSpotTabState._buildPlanFeatures`) et Premium (`_PremiumTabState._buildFeaturesList`) = classes State DISTINCTES. PawSpot : ligne ajoutée après la liste ; Premium : `{pawBadge:true, text}` → rendu `_pawBadgeMini()` au lieu de l'icône. 🌸 retiré de `shop_nearby_members` (6 langues) car le logo le remplace. flutter analyze clean + node -c (conversation/sitter) OK. ⚠️ users supprimés sur carte : profils hard-deletés donc absents ; reste défensif (suspended/banned exclus) — si Daniel revoit un « supprimé » c'est soit un compte encore actif (juste retiré des amis) soit ghost à confirmer.
- v490 (PUSH origin **7c496eb** ; build 23.1.490+490 → `Downloads/HoPetSit_v23.1.490.apk`) — **HOTFIX gros bug « champs gris » du wizard inscription (régression v489)**. Daniel : pages 2/3 des 3 rôles = blocs GRIS. ⚠️⚠️ **CAUSE RACINE + RÈGLE D'OR GetX** : mon restyle v489 avait écrit `_radiusDropdown` en `Obx(() => Builder(builder: (ctx) => …lit c.coverageRadius.value…))`. Une valeur réactive lue **DANS un `Builder` enfant** n'est PAS captée par l'`Obx` parent (le `.value` s'exécute hors de la zone de tracking de l'Obx) → GetX lève **« improper use of a GetX »** → **en RELEASE = ErrorWidget GRIS** (pas de crash rouge ; `flutter analyze` reste clean → invisible en dev). **JAMAIS `Obx(() => Builder(builder: …lit .value…))` ; lire le `.value` directement dans la closure de l'Obx.** FIX = retour au style champ SIMPLE et prouvé (`InputDecoration(border: OutlineInputBorder(), isDense: true)`) sur `_field`/`_dropdown`/`_radiusDropdown`/bios/`_PasswordField`/indicatif (méthode `_inputDecoration` v489 supprimée). Le restyle « champs arrondis remplis focus rôle » est ABANDONNÉ (trop risqué sans test runtime). Tous les AUTRES correctifs v489 conservés (retour page 1 `Get.to`, notifs `activeRoleAccent`, crash Google init try/catch, badge rose PawMap, mentions boutique). ⚠️ photo profil page 1 : le picker (CircleAvatar+caméra) existe déjà et marche — pas restylé comme la maquette (à confirmer avec Daniel s'il veut, prudemment). flutter analyze clean.
- v489 (PUSH origin **ebbdf86** ; build 23.1.489+489 → `Downloads/HoPetSit_v23.1.489.apk`) — lot « 6 bugs Daniel » + extras. **Notif** : `FriendController.accept` IDEMPOTENT (catch → refresh → si demande plus en attente = déjà ami → return true, plus de fausse « Erreur réessaie ») ; couleur notif = `AppColors.activeRoleAccent()` au lieu de la const orange `primaryColor` (vert/bleu/orange selon rôle courant) — `notification_card.dart` `_accentForType` fallback. **Inscription wizard** : flèche retour page 1 marchait pas car `sign_up_as.dart` ouvrait via `Get.off` (remplace) → passé en `Get.to` (3 rôles) → retour = choix du rôle ; refonte VISUELLE champs (`signup_wizard_screen.dart` helper `_inputDecoration` : arrondi 14r, fond carte, focus couleur rôle ; appliqué `_field`/`_dropdown`/`_radiusDropdown`/bios/`_PasswordField`/country picker) — 0 câblage touché. **CRASH Crashlytics #2 `loginWithGoogle:300`** (5 users, builds 139→485) : `_initializeGoogleSignIn()` appelé fire-and-forget dans `onInit()` SANS try/catch → `GoogleSignInException` de `initialize()` = exception async non attrapée = crash FATAL. FIX : tout `_initializeGoogleSignIn` enveloppé try/catch + `onError` sur le stream → non-fatal. (Crash #1 RxInterface : s'arrête à v470, plus actif, ignoré.) **PawMap mini-badge ROSE** (maquette « Map User Avatar ») : membres proches ni amis ni famille = bitmap canvas `_buildPawBadgeBitmap` (cercle rose #F06AA0→#E0568B + patte blanche `_drawWhitePaw` + point vert/gris en/hors ligne + 👑 doré si premium ; états normal/sélectionné/offline) posé en `Marker(nearby_$id, anchor center)` PAR-DESSUS le halo Circle (recoloré rose). Cliquable → `_onNearbyTap` (snackbar nom+rôle, idem v488). Visible UNIQUEMENT si `pawspotActive||premiumActive` (badge ET halo gatés). Amis/famille inchangés (photo). Cache : `_pawBadgeMarkers` + `_pawBadgeRev`/`_selectedNearbyId` + clé markers étendue. ⚠️ online par défaut true sauf `isOnline:false`/`online:false` du backend (pas de socket live pour les proches). **Boutique app+site** : ligne « 🌸 membres Paw Map proches (badge rose) » dans PawSpot + Paw Premium (app `shop_nearby_members` 6/6 ; site `pawpremium_feat_nearby`+`pawspot_feat_nearby` 6 langues). Bump trio v489 + badge admin v470→v489. flutter analyze clean + tsc site clean. **⏳ EN ATTENTE retour Daniel sur APK v489** (« j'espère c'est la fin des pb »).
- v487 (PUSH origin **e61599e** ; build 23.1.487+487 → `Downloads/HoPetSit_v23.1.487.apk`) — design only. **En-têtes Promeneur+Gardien recentrés comme owner** : héros restructuré de `Row(start)[avatar, Expanded(Column[chipRow, name, pill, stats])]` → `Column[chipRow(haut), Row(center)[avatar, Expanded(Column[name, pill])], statsRow(pleine largeur)]` (avatar CENTRÉ verticalement avec le nom, plus collé en haut). Mêmes widgets/handlers. Fait via 3 edits/fichier (Row→Column ; wrap name+pill dans Row center ; retirer Expanded+Row closers). **Conversation owner** : `titleSpacing: 0` sur l'AppBar → photo carrée du contact calée à gauche (ne chevauche plus l'icône 📞 partager-téléphone à droite). flutter analyze clean.
- v486 (PUSH origin **dba42f0** ; build 23.1.486+486 → `Downloads/HoPetSit_v23.1.486.apk`) — ✅ Daniel CONFIRME accueil OK (« tout fonctionne oufff »). Finition : (1) marqueur diagnostic « BUILD … » RETIRÉ de l'accueil. (2) Conversation 3 rôles : bulles déjà là (v480, owner orange/sitter bleu/walker vert via `AppColors.primaryColor`) ; ajout design sûr = avatar masqué sur messages ENVOYÉS (`if (!message.isFromCurrentUser)` devant CircleAvatar + son SizedBox, dans les 2 fichiers chat) → envoyés à droite sans avatar. Câblage intact. ⚠️ RESTE possible si Daniel veut la maquette « Conversation » À 100% : pilule « Partager mon numéro » stylée en BAS au-dessus de la saisie (actuellement mini-pill 📞 dans l'AppBar via `_onSharePhoneTap`) + restyle barre saisie (＋ rond/champ arrondi/➤) — refactor de l'écran TRÈS câblé, à faire en étape dédiée. flutter analyze clean.
- v485 (PUSH origin **2fdbdfc** ; build 23.1.485+485 → `Downloads/HoPetSit_v23.1.485.apk`) — **CAUSE RACINE du bug « accueil sans boutons » (v479→v484) ENFIN trouvée** (confirmée par marqueur on-screen : Daniel bien sur BUILD 484, donc bug RÉEL pas APK ancien). ⚠️⚠️⚠️ La grille 2×2 des services utilisait `Row(crossAxisAlignment: CrossAxisAlignment.stretch)` avec cartes `Expanded` DANS un `SingleChildScrollView` (hauteur non bornée) → force un **calcul de dimension intrinsèque sur enfants flex** → **exception layout SILENCIEUSE en RELEASE** (analyze clean, pas de crash debug visible) → la grille ET tout ce qui suit (boutons S'inscrire/Google/Se connecter) ne se dessinent pas. FIX : `stretch`→`start` sur les 2 rangées grille (`onboarding_screen`). **RÈGLE D'OR : JAMAIS `CrossAxisAlignment.stretch` sur un Row/Column contenant des `Expanded`/`Flexible` à l'intérieur d'un scroll (hauteur non bornée) → crash intrinsèque release.** Aussi : logo simplifié (sans Stack/RadialGradient halo), body=SingleChildScrollView+Column(min). ⚠️ marqueur « BUILD 23.1.485 » TEMPORAIRE encore affiché sous la tagline accueil → À RETIRER au prochain build une fois Daniel confirme OK. flutter analyze clean.
- v484 (build 23.1.484 → marqueur on-screen diagnostic) — confirmé Daniel sur le bon build, bug réel. Voir v485.
- v483 (PUSH origin **16768e6** ; build 23.1.483+483 → `Downloads/HoPetSit_v23.1.483.apk`) — écran ACCUEIL conforme maquette « App Opening » (Daniel : « ya aucun dégradé, fais comme le design »). `onboarding_screen` : fond ORANGE PLEIN clair `#f0562b→#ed4f25→#f87a52` (PLUS de fondu blanc) / brun `#3a201a→#161210` sombre. Bouton **S'inscrire = BLANC texte orange** (clair) / dégradé orange texte blanc (sombre) — avant = CustomButton orange INVISIBLE sur orange (cause du « pas de bouton »). Google = bouton verre translucide blanc `.16` + bord blanc `.4` + texte blanc (`_SocialButton` outlined recoloré ; Apple noir iOS). « Vous avez un compte ?/Se connecter » → BLANC (avant orange/gris illisibles). Séparateur « Ou continuer avec » RETIRÉ de l'accueil (absent maquette). Import `rounded_text_button` retiré. Câblage intact (SignUpAs/loginWithGoogle/loginWithApple/LoginScreen). flutter analyze clean. ⚠️ LEÇON : sur fond coloré plein, vérifier que boutons/liens ne sont pas de la même couleur (sinon invisibles).
- v482 (PUSH origin **bc45b70** ; build 23.1.482+482 → `Downloads/HoPetSit_v23.1.482.apk`) — **FIX URGENT écran ACCUEIL** (Daniel : « aucun bouton, bande noire qui bloque »). ⚠️ CAUSE RACINE : le pattern v481 `LayoutBuilder`+`ConstrainedBox(minHeight)`+`IntrinsicHeight`+`Expanded` PLANTE le rendu en RELEASE (IntrinsicHeight incompatible avec un enfant Expanded) → grille 2×2 + CTA invisibles, dégradé sombre nu. ⚠️⚠️ NE JAMAIS mettre un `Expanded` sous `IntrinsicHeight`. FIX infaillible : `SafeArea > Padding > Column` (hauteur bornée) + un simple `Expanded()` spacer avant les CTA, AUCUN scroll/IntrinsicHeight. Logo réduit 158→132 (halo 200→172) anti-débordement. Dark mode : dégradé adouci orange→brun #4A1C0C (plus de « filtre noir »). Câblage intact (SignUpAs/loginWithGoogle/loginWithApple iOS-only/LoginScreen). flutter analyze clean.
- v481 (PUSH origin **9d0e578** ; build 23.1.481+481 → `Downloads/HoPetSit_v23.1.481.apk`) — design only. **FIX CRITIQUE écran ACCUEIL** (régression v479) : grille 2×2 + boutons (S'inscrire/Google/Apple/Se connecter) ne s'affichaient plus (hors écran). `onboarding_screen.dart` body refait en pattern « remplir hauteur + footer bas + scroll si trop grand » : `LayoutBuilder`+`ConstrainedBox(minHeight: constraints.maxHeight)`+`IntrinsicHeight`+`Column` avec `Expanded(child:SizedBox)` spacer avant les CTA (remplace `SizedBox(32)`). ⚠️ pattern à réutiliser pour tout écran « footer collé + scroll fallback ». Câblage intact. **EN-TÊTE PROFIL v2 (1)** : (a) cloche `ProfileNotificationBell` icône blanc→**#FFCB2E** (jaune doré, 3 rôles) ; (b) carte blanche « Note/Avis/Services » du SITTER masquée (doublon avec stats en-tête v478) → `_buildSitterStatsRow`+`_statItem` SUPPRIMÉS + import `my_reviews_screen` retiré ; walker n'avait PAS cette carte (juste tuile « Mes avis » conservée) ; (c) gradient en-tête = hauteur contenu (minHeight 58 < contenu réel → pas de vide). flutter analyze clean.
- v480 (PUSH origin **d10bb95** ; build 23.1.480+480 → `Downloads/HoPetSit_v23.1.480.apk`) — DESIGN ONLY (iso-fonctionnel, 0 logique touchée). **CONVERSATION en bulles** (maquette « Conversation Page (1) ») : `individual_chat_screen.dart` (owner) + `sitter_individual_chat_screen.dart` (sitter/walker) — message texte wrappé dans une BULLE (`isFromCurrentUser` ? couleur rôle `AppColors.primaryColor` texte blanc, coins bottomRight 4 : `AppColors.card` blanc bord gris, coins bottomLeft 4) + Column `crossAxisAlignment` end/start pour aligner droite/gauche. Reçu garde l'offset left:41 (avatar), envoyé left:0. TOUT le câblage conservé (long-press supprimer/signaler, bouton Effacer, pièces jointes+signaler, msg supprimé, Traduire, cartes suivi/adresse/numéro, saisie). 2 edits/fichier (Column align + wrap texte). **INSCRIPTION wizard** (`signup_wizard_screen.dart`, maquette « Inscription Flow ») : bouton bas → DÉGRADÉ couleur rôle `[_accent, lerp(_accent,black,.18)]` + ombre (mêmes `_onNext`/`isLoading`) ; en-tête compact paw 54→46 + ombre, titre 17→16/900. Reste déjà conforme (stepper ✓, chips wrap, `_accent` par rôle). flutter analyze clean. ⚠️ choix Daniel : « les deux dans un seul build ».
- v479 (PUSH origin **7a451ba** ; build 23.1.479+479 → `Downloads/HoPetSit_v23.1.479.apk`) — écrans d'OUVERTURE refondus (maquette « App Opening »). **Accueil** (`views/onboarding/onboarding_screen.dart`) : logo héros squircle blanc 124px + HALO radial derrière (blanc clair/orange sombre) + bord blanc + ombre ; sous-titre = `onboarding_services_subtitle` ; 3 anciennes cartes verticales → GRILLE 2×2 des 4 services (`_FeatureCard` réécrite horizontale : icône carrée 46 teintée + titre + 1 ligne) Pet-sitting/PawMap/PawFollow/PawSpot (PawSpot doré `gold:true`), dark-mode (#241D1A/#352A25). **Connexion** (`views/auth/login_screen.dart`) : carte blanche centrée → bandeau dégradé orange (logo squircle 60 + « Bon retour 👋 » + sous-titre + 🐾 filigrane). Câblage 100% intact (S'inscrire, Google `loginWithGoogle`, Apple `loginWithApple` iOS-only `Platform.isIOS`, `handleLoginWithNavigation`, mdp oublié, langue, thème). i18n +6 clés ×6 (marques PawMap/PawFollow/PawSpot = littéraux noms propres). Anciennes clés `onboarding_feature_*` devenues inutilisées (laissées, sans risque). flutter analyze clean.
- v478 (PUSH origin **fd25897** ; build 23.1.478+478 → `Downloads/HoPetSit_v23.1.478.apk`) — refonte EN-TÊTE PROFIL v2 (maquette « Header Redesign v2 »). Walker(vert)+Sitter(bleu) : pastille statut `profile_status_available_walk/sit` + 2 stats RÉELLES = « jours actifs » (calculé client-side `_daysActive(ProfileModel.createdAt)` = nb jours depuis inscription, AUCUN champ backend ajouté car `createdAt`/`rating` déjà exposés par le serializer + `ProfileModel`) + « note » (`profile.rating`, clé `stat_rating`). Helpers `_daysActive`/`_heroStatusPill`/`_heroStat` ajoutés aux classes sitter+walker. Avatar agrandi ~84 (sitter r31→41, walker 62→84, owner 70→84 + image interne 60→84) + nom 26-28sp ×3. Owner : liseré doré PawBoost RETIRÉ du héros (Obx boost wrapper supprimé → `_buildOwnerHero` direct ; « pas de contour jaune »). Câblage intact (cloche/photo/calendrier/portefeuille/modifier animal/puce). i18n +3 clés ×6. Nettoyage sitter : `_localizeService`+import `active_benefits_row` retirés (inutilisés). ⚠️ `ActiveBenefitsRow` reste utilisé chez OWNER (name column) — import owner conservé. flutter analyze clean.
- v477 (PUSH origin **5d55a34** ; build 23.1.477+477 → `Downloads/HoPetSit_v23.1.477.apk`) — écran CONVERSATION (individual chat) : zone messages teintée par rôle (maquette « Conversation Page »). `individual_chat_screen.dart` (owner) Expanded(ListView) wrappé Container `#FDF0EB` ; `sitter_individual_chat_screen.dart` Container `primaryColor.withValues(alpha:0.05)` (bleu sitter/vert walker). AUCUN câblage touché (traduire/effacer/partage adresse+numéro/suivi/voir carte/saisie intacts). ⚠️ RESTE proposé à Daniel : conversion en VRAIES bulles gauche/droite (incoming blanc, envoyé coloré gradient rôle) = refactor structurel profond de cet écran très câblé (layout actuel = plat : avatar+nom+heure+texte left-aligned, pas de bulles) — à faire en étape dédiée pour ne rien casser. flutter analyze clean.
- v476 (PUSH origin **4838034** ; build 23.1.476+476 → `Downloads/HoPetSit_v23.1.476.apk`) — page Messages/Chat redessinée selon maquette HTML Daniel (« Messages Page.dc »). Cartes conversation owner (`chat_screen.dart`) + sitter/walker (`sitter_chat_screen.dart`) : radius 14→20 + liseré accent rôle (`primaryColor` .12) + ombre ; pastille présence TOUJOURS visible (vert #1F8A4C en ligne / gris #B3AFA8 hors ligne, top-right) ; nom w400→w800 15sp ; bouton supprimer = carré ROSE #FDE7E3 corbeille #E2553B (au lieu du pill « Effacer » texte). Bouton Nouvelle conversation garde l'accent. Barre menu bas INCHANGÉE (orange). flutter analyze clean.
- v475 (PUSH origin **27e2578** ; build 23.1.475+475 → `Downloads/HoPetSit_v23.1.475.apk`) — en-têtes profil refondus selon la MAQUETTE HTML de Daniel (« Header Redesign.dc.html »). Les 3 hero (owner/sitter/walker) : **🐾 en filigrane** (Positioned Text 150sp opacity .10, top-right, Transform.rotate .31) + **cercle déco** translucide (bottom-left) ajoutés en fond (helpers `_heroPaw`/`_heroDecoCircle` dans CHAQUE fichier). **Owner refonte complète** : chip rôle frosted (logo+rôle, helper `_heroRoleChip`) + cloche, avatar inline anneau+caméra (70, plus Positioned), nom 22/900, ActiveBenefitsRow, puis **`_buildOwnerPetsStrip`** (ListView horizontal câblé sur `MyPetsController.pets` : avatar+nom+race, défile si >3). Sitter/walker : 🐾+cercle ajoutés en wrappant le `child: SafeArea` dans un `Stack(children:[_heroPaw(),_heroDecoCircle(), SafeArea(...)])` (technique : Edit A ouvre le Stack avant SafeArea, Edit B ajoute `],),` après la fermeture SafeArea avant la fermeture Container). Câblage 100% conservé. flutter analyze clean.
- v474 (PUSH origin **b371d05** ; build 23.1.474+474 → `Downloads/HoPetSit_v23.1.474.apk`) — refonte MODERNE en-têtes profil par rôle. Les 3 gradient Containers (owner orange/sitter bleu/walker vert) : ajout `clipBehavior: Clip.antiAlias` + `borderRadius: BorderRadius.vertical(bottom: Radius.circular(30.r))` + `boxShadow` coloré (profondeur HD) = en-tête CARTE arrondi au lieu du bandeau plat. minHeight 56/58 + avatars 60/r31/62 conservés (compact). Câblage 100% préservé (avatar tap→photo, nom, pastille rôle, KYC, cloche, ActiveBenefitsRow, service pills). Cadre boost doré owner arrondi aussi. walker decoration dé-`const`isée (borderRadius `.r` non const). flutter analyze clean.
- v473 (PUSH origin **02fde6c** ; build 23.1.473+473 → `Downloads/HoPetSit_v23.1.473.apk`) — tarifs promeneur + avatars profils. **Tarifs fiche promeneur** : la ligne durée était « Promenade X min » EN DUR FR (oubli v472) + anciens paliers 30/60/90/120 ; corrigé → filtre `[30,60,120]` + `_walkDurationLabel` « 30 min / 1 h / 2 h » (90 masqué, neutre toutes langues). **Avatars profils** réduits encore (owner 70→60, sitter r36→31, walker 72→62) pour raccourcir le bandeau. ⚠️ **IMPORTANT** : Daniel teste souvent un APK ANCIEN → bandeaux profils « géants », fiche FR, tarifs 30/60/90/120 = symptômes d'un build < v472/v473. Tout est déjà corrigé : en-têtes minHeight 56/58 (v471), déconnexion 110+inset (v468), âge animal champ+Caractéristiques+publication (v471). Toujours lui dire d'installer le DERNIER APK. flutter analyze clean.
- v472 (PUSH origin **549fddd** ; build 23.1.472+472 → `Downloads/HoPetSit_v23.1.472.apk`) — pastille vérifié + i18n fiche promeneur. **Pastille « vérifié »** : `walker_detail_screen` utilisait `w.verified` (vrai pour tous) → badge à tort ; passé à `w.identityVerified` (KYC payé 3€), cohérent avec fiche sitter (`identityVerified`) + cartes recherche. **Fiche promeneur FR en dur** (Daniel : ES sort en FR) : « À propos/Localisation/Langue parlée/Tarifs/Promenade X min/Pas encore d'avis » étaient hardcodées FR → restaient FR en espagnol ; remplacées par i18n (nouvelles clés `walker_detail_about/language/rates/walk_minutes` ×6 + réutilise `sitter_detail_location`+`candidates_no_reviews`). **Cadre gris « Signaler autour de moi »** : `create_report_sheet` n'instancie AUCUNE GoogleMap → artefact d'un vieux build (v470), absent du code courant (à reconfirmer sur v472). flutter analyze clean.
- v471 (PUSH origin **d9a1237** ; build 23.1.471+471 → `Downloads/HoPetSit_v23.1.471.apk`) — âge + logo accueil + cadres profils. **ÂGE CAUSE RACINE (après 5 versions de tâtonnement)** : le formulaire « Modifier l'animal » (`edit_pet_screen.dart` + `edit_pet_controller`) n'avait **AUCUN champ Âge visible** — seulement un sélecteur de **date de naissance** souvent non rempli → `pet.age` ET `pet.dob` vides → `serializeAge`/`resolvePetAgeYears` renvoient '' → âge absent PARTOUT (fiche chien + annonce + web). FIX = champ « Âge (années) » (`edit_pet_age_years`) ajouté lié à `ageController` (déjà soumis ligne ~361 + sauvé backend whitelist `age` Number 0-60, prioritaire sur dob) ; chargement nettoyé `pet.age.replaceAll(RegExp(r'[^0-9]'),'')`. ⚠️ animaux existants sans âge = ré-éditer pour saisir l'âge. **Accueil owner** : FAB « + » remonté 96→120. **Cadres 3 profils RÉDUITS fort** (« juste sous les badges ») : minHeight 72/76→56/58, padding vertical 6→3, nom 22→18.sp, avatar owner 80→70 (top 30→22). flutter analyze clean. (Pas de changement backend/site fonctionnel ce tour ; trio bumpé v471.)
- v470 (PUSH origin **243cb9f** ; build 23.1.470+470 → `Downloads/HoPetSit_v23.1.470.apk`) — logo PawMap officiel + bugs round 2. **Logo PawMap officiel** (fourni Daniel = carré orange arrondi + carte/pin/patte) : web = logo COMPLET avec texte (`website/public/pawmap_logo.svg` + `pawmap_logo_orange.svg`, plus aucun bleu/vert) ; app AppBar = `assets/images/pawmap_logo_orange.svg` version **glyphe seul** (sans texte) rendu SANS colorFilter (le `srcIn` v469 écrasait tout en « point orange »). **Couronne premium TOUS** : `fetchUserMini`+`/diagnose` renvoient `isPremium` ; `FriendProfile.isPremium` ; `premiumMemberIds` (paw_map ~1412/1838) = union famille+amis premium. **Carte agrandie suivi** : calque a `_expandedCtl` ; `_animateFollowCamera` cible le calque si `pawMapExpanded`. **Censure NON-DESTRUCTIVE annonces** (corps+notes+commentaires) : `sanitizePost` censure à la LECTURE (`_cleanPostText`), write stocke BRUT (postController create/update/media + getNearby) ; SAFE_WORDS élargi (gentil/sage/calme… 6 langues) ; ⚠️ annonces déjà étoilées à RECRÉER. **Âge** : `resolvePetAgeYears` dérive l'âge des années depuis `dob` si `pet.age` absent (4 maps postController) → publication app+web ; compact owner feed affiche l'âge aussi (`pet_post_card` `_buildPetsStrip`). **Bas de page** : chat sitter/walker FAB remonté (manquait), PawMap Signaler 132/AutourDeVous 116+réduit 2 entrées/itinéraire 136, pickers agrandi socle +48. **Profils ×3 réduits** : minHeight 100→72/76 + avatars 100→80(owner)/84→72(walker)/r42→36(sitter). flutter analyze clean + node -c + tsc OK.
- v469 (PUSH origin **da07abf** ; build 23.1.469+469 → `Downloads/HoPetSit_v23.1.469.apk`) — lot fin (partiel). **Bas de page TOUS onglets** : `extendBody` reste **true** (sinon resize→blanc carte) ; dégagement bas ajouté partout (chat FAB remonté, réservations 20→110+inset ×3, profils ×3, accueils ×3, PawMap Signaler/Autour de vous/itinéraire/bandeaux relevés au-dessus du menu en mode normal `+84.h` si `!pawMapExpanded`). **Censure NON DESTRUCTIVE PawSpot** : `pawSpotRoutes` stocke le BRUT, censure à la lecture (`spotJson` via `_moderateSpot` + GET comments) → rétroactif ; ⚠️ spots déjà stockés censurés (paseo perros) à RECRÉER. **Profils hero** minHeight 130→100 + padding 12→6 ×3. **Logo AppBar PawMap ORANGE** (`colorFilter #F2741B` sur pawmap_logo.svg). **Annonce caractère** : âge affiché à côté de la race (`pet_post_card` ~1195, `_ageLabel(p.age)`). flutter analyze lib clean + node -c OK.
- v467 (PUSH origin **17c91b7** ; build 23.1.467+467 → `Downloads/HoPetSit_v23.1.467.apk`) — bas de page + finitions. **Barre Samsung transparente** (boutons cachés derrière) : `systemNavigationBarContrastEnforced` false→**true** (main.dart, 2 endroits) = barre opaque. **Chat « Nouvelle conversation »** : FAB remonté (`Padding bottom 78.h`) au-dessus du menu flottant. **PawMap** : (a) viseur Tag Spot = repère central **ROSE #EC1E79** (vs rouge signalement) via `_buildCenterReticle` lit `_pickingSpotPos` ; (b) bandeau spot picker remonté `bottom: 16 + viewPadding.bottom` (plus derrière la barre) ; (c) `pawspot_pick_confirm` = « Valider le spot » ×6 ; (d) mode agrandi : **retiré** le FAB Signaler bas-gauche (ne reste que les 2 boutons droite). **i18n profil prestataire** (étaient EN) : `service_sit_home/sit_owner/daycare/meds/vet_transport` + `pet_type_rodent/reptile` ×6 (clés canoniques signup = sit_home/sit_owner/daycare/meds/vet_transport ; affichées via `translateServiceType`→`service_<norm>` et `_petTypeLabel`→`pet_type_<norm>` dans `service_provider_detail_screen.dart`). **Profils owner/sitter/walker** : rectangle en-tête 160→**130** (avatar owner top 60→30). **SITE** : annonce âge affiché seulement si > 0 (`posts/page.tsx`) ; **retiré le gros bouton « Ouvrir Paw Map »** du hero accueil (`page.tsx`, import PawMapCTA retiré). flutter analyze lib clean + node -c + website tsc OK. ⚠️ RESTE possible : audit exhaustif de TOUS les bas d'écran (Daniel veut « aucun bouton derrière » partout) — fait les cas signalés, le reste au besoin.
- v466 (PUSH origin **f5e7bfd** ; build 23.1.466+466 → `Downloads/HoPetSit_v23.1.466.apk`) — **VRAIE CAUSE RACINE « Impossible d'annuler cette réservation »** (le bug 72h récurrent v161/v254/v449/v463 enfin résolu à la source) : `selfCancelWithRefund` faisait `booking.payoutStatus='cancelled'` (quand payout='scheduled', cas de TOUTE résa payée à venir) + `booking.cancelledBy='walker'` (annulation promeneur), mais **ni 'cancelled' ni 'walker' n'étaient dans les enums du schéma `backend/src/models/Booking.js`** → Mongoose `ValidationError` sur `booking.save()` → **500** → app affichait l'erreur générique. Ce n'était NI le check 72h NI la permission. FIX : `payoutStatus` enum += `'cancelled','refunded'` ; `cancelledBy` enum += `'walker'`. ⚠️ LEÇON : un 500 sur save() = vérifier les ENUMS du schéma avant de blâmer la logique. **+ Refonte carte « Mes réservations »** (owner/sitter/walker) : carte surélevée (ombre), coins 18, liseré couleur du rôle (owner orange/sitter bleu/walker vert) ; owner en-tête bandeau orange pâle ; câblage 100% conservé (tap→détails, Payer/Annuler 72h, carte confirmation service, invite à noter). flutter analyze lib clean + node -c OK.
- ✅ **v464 confirmé OK par Daniel** : nouveau menu visible + mode agrandi marche (« sa marchheeeee »). Le pattern calque-overlay v464 est validé.
- v465 (PUSH origin **bfe40f4** ; build 23.1.465+465 → `Downloads/HoPetSit_v23.1.465.apk`) — lot finitions. **PawMap agrandi** : en mode agrandi le menu du bas est MASQUÉ (RxBool global `pawMapExpanded` dans `frontend/lib/utils/map_ui_state.dart`, observé par `stacked_navigation_wrapper` via Obx) → plein écran, Signaler/Tag Spot/bandeaux de validation enfin visibles (avant cachés derrière le menu) ; bouton Agrandir/Réduire fond ROSE intense `#EC1E79` en agrandi. **Menu bas COLLÉ à la barre Samsung** : retiré la bande de sécurité colorée v464, padding bas = `2 + viewPadding.bottom`. **FriendsScreen** (nouvelle conversation) : marge basse = `inset + 24` (dernier élément/bouton plus caché). **Âge « 0m ans »** : `petAgeDisplay` (pet_age_format.dart) retourne '' pour tout 0/inconnu + tolère formes malformées ; sous-titre + ligne Âge masqués si vide ; backend `serializeAge` ne renvoie plus « 0m »/« 0y ». **Sur-censure PawSpot** : `textModerationService` → liste BLANCHE `SAFE_WORDS` (mots courants fr/en/es/de/it/pt jamais masqués, ex. paseo/perros/para) vérifiée AVANT détection + `adminRoutes.refreshBannedWords` n'éclate plus les phrases en fragments < 4 lettres (« de »/« la »…). Testé : « buen paseo para perros, cerrado » intact, « fuck » masqué. **Traits web EN→FR** : nouveau `website/src/lib/petTraits.ts` (12 traits ×6 langues), `posts/page.tsx` affiche `petTraitLabel(tr, lang)` au lieu de la clé brute (le formulaire d'édition `pets/page.tsx` garde les clés). **Profils owner/sitter/walker** : rectangle d'en-tête `minHeight` 200→160 (moins de vide sous les badges, avatar non rogné). flutter analyze lib clean + node -c + website tsc OK.
- ⚠️ **PATTERN SÛR « agrandir la carte »** (v464, après 2 casses v451/v458) : NE JAMAIS redimensionner la GoogleMap existante (= blanc Android). Le mode agrandi = un CALQUE `Positioned.fill` posé PAR-DESSUS la PawMap normale (qui reste montée + intacte), avec sa PROPRE GoogleMap dédiée plein écran (instance créée à pleine taille = pattern écran v457 qui marchait). Réduire = retirer le calque → carte normale réapparaît telle quelle. Échec isolé au mode agrandi (mode normal jamais touché).
- v464 (PUSH origin **774127c** ; build 23.1.464+464 → `Downloads/HoPetSit_v23.1.464.apk`) — **PawMap mode « carte agrandie » SÛR + zone de sécurité menu**. (1) Mode normal INCHANGÉ + petit bouton compact « Agrandir » à côté de la bannière « Suivi en direct » (barre du haut, `_buildTopShareRow`/`_buildExpandPill`, RxBool `_mapExpanded`). (2) Agrandi = calque `Positioned.fill` (`_buildExpandedMapOverlay`) par-dessus, GoogleMap dédiée plein écran (Material fond rôle anti-flash) ; en haut [Suivi en direct][Réduire], à droite 2 mini-FAB empilés (Signalement→`_startReportPicking`, Tag Spot→`_startSpotPicking`, heroTags `expReportMini`/`expTagSpotMini`), bas-gauche FAB Signaler (`_buildReportFab(heroTag:'expReportFabBL')` — paramétré pour éviter conflit Hero avec la carte normale dessous) ; reticule+bandeaux validation réutilisés ; gating abo inchangé. (3) Menu bas (`stacked_navigation_wrapper`) : `viewPadding.bottom` (fiable nav gestuelle) + gap 10 + **bande de sécurité colorée sous le menu = `AppColors.scaffold(context)` = couleur du rôle** (owner orange pâle/sitter bleu/walker vert), dark-aware. i18n `pawmap_expand_short`×6. **Vérifié libapp.so** : expReportFabBL/expTagSpotMini/pawmap_expand_short présents. flutter analyze lib clean. ⚠️ 2 GoogleMaps coexistent PENDANT l'agrandi (normale couverte + calque) — accepté (risque isolé au mode agrandi). Backup carte sûre tjs : `git checkout pawmap-v450-stable -- frontend/lib/views/map/paw_map_screen.dart` ou `/c/tmp/paw_map_screen.v463-backup.dart`.
- v463 (PUSH origin **260e2b4** ; build 23.1.463+463 → `Downloads/HoPetSit_v23.1.463.apk`) — **FIX annulation 72h « ne marche pas » + pop up ANGLAIS** (bug récurrent v161/v254/v449). CAUSE RACINE : le frontend RE-CALCULAIT l'éligibilité 72h depuis `booking.date` seul (minuit, sans timeSlot, fuseau local) ≠ backend `resolveBookingStartDate` (date+timeSlot) → divergence → backend refuse (409) affiché TEL QUEL = anglais ; pire, l'écran détail faisait `DateTime.parse()` nu qui plantait sur « dd/MM/yyyy » → bouton Confirmer caché → annulation impossible. FIX = **backend AUTORITAIRE** : `sanitizeBooking` (backend/src/utils/sanitize.js) expose `canSelfCancel`+`hoursUntilStart` calculés EXACTEMENT comme `selfCancelWithRefund` (payé + statut non terminal + >72h, même resolveBookingStartDate répliqué inline). `BookingModel` lit ces champs + getters `isSelfCancelEligible`/`hoursUntilStartResolved` avec filet local robuste (ISO + dd/MM/yyyy) si backend pas déployé. 5 écrans (détail+liste owner/sitter/walker) utilisent les getters (plus de recalcul). 3 controllers : erreur → message LOCALISÉ (`cancel_72h_closed_message` si 409, sinon `cancel_72h_error`) au lieu du message backend brut anglais. Retiré import `intl` devenu inutile ×3 listes. flutter analyze lib clean + node -c OK. ⚠️ RÈGLE : ne JAMAIS afficher `error.message` backend brut dans un snackbar (c'est de l'anglais) — mapper par statusCode vers une clé i18n.
- v468 (PUSH origin **711615d** ; build 23.1.468+468 → `Downloads/HoPetSit_v23.1.468.apk`) — **menu bas = BARRE PLEINE LARGEUR « de bout en bout »** (Daniel). `stacked_navigation_wrapper` : la pilule flottante (marges 12, radius 28) → Container pleine largeur collé au bas, coins arrondis EN HAUT seulement (radius 24), ombre vers le haut, inset Samsung en padding bas. Toujours masqué en mode carte agrandie (`pawMapExpanded`). flutter analyze clean.
- ⚠️ **LE VRAI menu bas = `frontend/lib/widgets/stacked_navigation_wrapper.dart`** (monté comme `bottomNavigationBar` via IndexedStack). `custom_navigation_bar.dart` (CustomNavigationBar + _CenterPawMapButton) = **CODE MORT jamais instancié → tree-shaké** : NE PAS l'éditer pour le menu, les changements n'apparaîtront JAMAIS. Preuve dead-file : v460/v461 libapp.so byte-identiques + strings menu (F2741B) absents du .so malgré présence source.
- v462 (PUSH origin **ebc33b4** ; build 23.1.462+462 → `Downloads/HoPetSit_v23.1.462.apk`) — **FIX cause racine : nouveau menu enfin VISIBLE**. Le nouveau menu (maquette Claude Design) était appliqué depuis v460 à `custom_navigation_bar.dart` = code mort → invisible. Fix = appliqué au VRAI widget `stacked_navigation_wrapper.dart` (pill blanc flottant radius 28 + icônes duotone SVG `SvgPicture.string` actif #F2741B/inactif #7D7D82 + bouton central « Paw Map » pilule orange gradient F2741B→E0660F, viewBox `0 0 28 28` carte+pin+patte). **Vérifié dans libapp.so** : `0 0 28 28`(1), `F2741B`(6), `Paw Map`(1) → compilé cette fois. Logique préservée : IndexedStack, `_onTap` (refresh badges + reload Chat/SitterChatController + syncChatBadgeFromServer), badge messages rouge (unreadChat), badge réservations vert role-aware (`_bookingsBadgeCount` pendingActionCount), menu tjs visible (`extendBody`). **+ carte** : `paw_map_screen.dart` revert v456 (2e08bcc) = supprime l'agrandissement en place cassé, garde point rouge + « J'ai vu cet animal ». flutter analyze lib clean.
- v460 (PUSH origin **e1db3d9**) — ⚠️ OBSOLÈTE/INEFFICACE : nouveau menu appliqué à `custom_navigation_bar.dart` (code mort) → n'apparaissait pas. Voir v462 pour le vrai fix.

- v459 (PUSH origin **2e4f795** ; build 23.1.459+459 lancé b1jlfqwtq → `Downloads/HoPetSit_v23.1.459.apk`) — **FIX carte agrandie BLOQUÉE** (Daniel photo : PawMap s'ouvrait bloquée en agrandi, aucun contrôle du haut, impossible d'en sortir ; la carte s'affichait bien = pas de blanc). Cause : la surimpression `_buildTopControlsOverlay` (v458) était dans un Stack PARENT, frère du `Positioned.fill` de la GoogleMap → sur Android la **surface native de la carte recouvrait ce calque** → contrôles invisibles + non cliquables. Preuve : les FAB Signaler/Tag spot, placés DANS le même Stack que la carte, s'affichaient bien. **Fix = déplacer la surimpression DANS le même Stack que la carte + les FAB** (dernier enfant → au-dessus). ⚠️ RÈGLE : tout overlay Flutter au-dessus d'une GoogleMap Android DOIT être dans le MÊME Stack que le widget GoogleMap, pas dans un Stack parent. flutter analyze clean.

- v458 (PUSH origin **962a218** ; build 23.1.458+458 lancé bkshc8trx → `Downloads/HoPetSit_v23.1.458.apk` ; backup sûr tjs = tag `pawmap-v450-stable` + `HoPetSit_v23.1.457.apk`) — **1. Bouton central « Paw Map »** refait comme la maquette : carré ARRONDI orange chaud (gradient ambre→orange `0xFFFAA64A`→`0xFFF17C1B`, radius 20) + icône SVG `assets/images/pawmap_nav.svg` (carte pliée+pin+patte) + texte (`custom_navigation_bar.dart`, import flutter_svg). **2. « Agrandir » EN PLACE et SÛR** (Daniel rejette le plein écran v457) : refonte body PawMap → GoogleMap en `Positioned.fill` (taille FIXE permanente → JAMAIS redimensionnée → plus de blanc Android) + contrôles du haut EN SURIMPRESSION (`_buildTopControlsOverlay`) ; rangée `_buildShareAndExpandRow` = « Partager ma position » (Expanded, mi-largeur) + bouton ROSE Agrandir/Réduire (`_mapExpanded` RxBool) toujours visible ; agrandi → masque 4 cartes+filtres+zoom+autour-de-vous, garde 2 boutons haut + Signaler + rond doré `_buildTagSpotFab` (PawSpot) ; zoom +/-/géoloc déplacés centre-droit. FullScreenMapScreen (v457) plus appelé (fichier orphelin gardé). i18n `pawmap_reduce_map`/`pawmap_tag_spot`/`pawmap_fullscreen_title` ×6. **3. Traductions PT** : FIX bug `pet_photo_delete_failed` orpheline (avalait `edit_pet_age_behavior`) + 13 mistraductions PT (button_apple Maçã→Apple, common_no No→Não, filter_clear, time_minutes_ago, location_map, create_pet_category_dog/cat, status_all_label, send_request_end_label, booking_agreement_at, publish_request_service_*, onboarding_app_title). flutter analyze lib clean + node -c OK. ⚠️ AGRANDIR = restructure body RISQUÉE non testée (KO Daniel) → si carte recasse, réinstaller v457 / `git checkout pawmap-v450-stable -- frontend/lib/views/map/paw_map_screen.dart`. RESTE traductions : Cluster B ~30 clés snackbar EN sur es/de/it/pt + garble DE `snackbar_text_successfully_switched...`.

- v456/v457 (réintégration progressive après le fix carte blanche, base stable v450 confirmée) — **v456** (PUSH **2e08bcc** ; APK `HoPetSit_v23.1.456.apk`) réintègre sur la base v450 : **AA point rouge** (reticle `_buildCenterReticle` + `_onCameraMove` sync centre + suppression marqueur draggable + validate=`_currentCenter`) ✅ confirmé OK par Daniel, et **AB bouton « J'ai vu cet animal »** (`_showReportBottomSheet` si `report.type==lostPet`). **+ push c0d535f backend** : le bouton AB n'apparaissait pas car `lost_pet` était PREMIUM → invisible aux free users dans `/nearby` ; fix `PUBLIC_VISIBLE_TYPES=[...FREE_REPORT_TYPES,'lost_pet','found_pet']` pour la VISIBILITÉ (création reste premium) → animal perdu visible+aidable par tous. **v457** (PUSH **c95a131** ; build 23.1.457+457 btmg7x212 → `HoPetSit_v23.1.457.apk`) = **« agrandir la carte » EN VERSION SÛRE** : NOUVEL écran dédié `views/map/fullscreen_map_screen.dart` (GoogleMap plein écran créée 1 fois, JAMAIS redimensionnée, AUCUN Obx → pas de blanc/crash ; position live + spots + signalements figés + bouton Réduire) ; PawMap = simple bouton « Agrandir » (Positioned isolé haut-gauche, pas de restructuration) → `Get.to(FullScreenMapScreen)`. NB cause racine confirmée du blanc v451 : l'ancien agrandir cachait les contrôles au-dessus → REDIMENSIONNAIT la GoogleMap → rendu Android blanc. i18n `pawmap_fullscreen_title` ×6. RESTE : 2 crashs Firebase (Obx improper « Modifier profil » v257-450 + GoogleSignIn canceled en fatal). cf [[maps-blank-google-cloud-key]].

- v455 (PUSH origin **7fe2fd4** ; tag **`pawmap-v450-stable`** = BACKUP ; APK `Downloads/HoPetSit_v23.1.455.apk` + copie `HoPetSit_STABLE_carte_OK_v455.apk`) — **RÉSOLU : carte blanche = RÉGRESSION CODE v451+, confirmé par Daniel** (« à 450 tout marchait » → carte revenue après restauration). Fix = `git checkout 97069ca -- frontend/lib/views/map/paw_map_screen.dart` (restaure l'écran carte EXACT de v450) + annulation de la tentative v454 `useAndroidViewSurface` (main.dart+deps revert — n'était PAS la cause). **Coupable suspecté par Daniel = le mode « agrandir la carte » (`_mapExpanded`, chantier O/U v451)**. ⚠️ Diagnostic : web map OK ≠ Android map OK (clés/restrictions différentes) ; mes 1ers diagnostics (Google Cloud billing, puis rendu TLHC) étaient FAUX — c'était bien mon code. Retirés temporairement de la carte (à réintégrer 1 par 1 AVEC test à chaque étape, en évitant/testant à part l'agrandir) : point rouge placement (AA v453), bouton « J'ai vu cet animal » (AB v453), empreinte itinéraire (X v452), mode agrandir (O/U v451). 2 crashs Firebase restants à traiter : `improper use of a GetX` (Obx pré-existant v257-450, PAS dans l'écran carte — probablement écran Modifier profil) + `GoogleSignInException(canceled)` remonté en fatal (bénin = annulation login). RESTAURER si re-casse : `git checkout pawmap-v450-stable -- frontend/lib/views/map/paw_map_screen.dart`.

- v453 (PUSH origin **5a16cab** ; build 23.1.453+453 lancé bvs272ns3 → `Downloads/HoPetSit_v23.1.453.apk` + PDF iOS v453) — 4 chantiers. **Y. SITE = Paw Map CTA phare partout** : nouveau `website/src/components/PawMapCTA.tsx` (pilule orange **#FF6A00** hover #E85F00, glyphe carte+patte SVG blanc, tailles `hero`/`compact`, href = `useAuth().user` ? `/map` : `/login?redirect=%2Fmap`) ajouté Header (sticky→toutes pages) + Footer + hero accueil + hero /pawmap (remplace bouton vert) ; login lit `redirect` via `window.location.search` (pas de useSearchParams, push si commence par `/`). **Z. SITE bannière téléchargement** : `DownloadAppBanner.tsx` dans `layout.tsx` (barre fine desktop `bg-ink` + bouton flottant mobile `fixed bottom-4 right-4`, → `/download`, non masquable) + 4 lignes marketing sous /pawmap (`pawmap_msg_*`) ; clés web `cta_open_pawmap`/`dl_banner_title`/`dl_float`/`pawmap_msg_*` ×6. **AA. APP viseur POINT ROUGE fixe** (`paw_map_screen.dart`) : Daniel veut déplacer la CARTE sous un repère rouge central (façon Uber), plus de pin draggable — marqueur `spot_picker` supprimé, overlay `_buildCenterReticle()` (point rouge #DC2626 + halo + aiguille, IgnorePointer, Center) unionné dans le Stack si `_pickingSpotPos||_pickingReportPos`, position suit `_onCameraMove`→`_pickedSpotPos=pos.target` ; à Valider le repère disparaît (flag false) + emoji final via reload existant ; hints `pawspot_pick_hint`/`pawmap_report_pick_hint` réécrits « déplacez la carte » ×6. **AB. « Aider animal perdu » câblé** : backend `mapReportRoutes` `/:id/confirm` passé `requirePremium`→`attachPremium` + gate Premium SEULEMENT si `type!=='lost_pet'` (aider = GRATUIT pour tous) ; sur sighting lost_pet → `sendNotification(type:'lost_pet_sighting')` prévient le PROPRIÉTAIRE (cloche+push+email, template ajouté aux 6 `locales/*/notifications.json`) ; app `_showReportBottomSheet` : si `report.type==ReportTypes.lostPet` bouton plein rose « J'ai vu cet animal » (`pawmap_lost_seen_btn`) + encart `pawmap_lost_help_hint` + snackbar `pawmap_lost_seen_thanks` (3 clés ×6). flutter analyze lib clean + node -c OK + admin JS 3/3 + website `next build` OK + `tsc --noEmit` OK + i18n parité 6 langues. NB AB : pas de chat helper↔reporter (contact = via notif/map) ; turn-by-turn Waze complet (v452) tjs en attente d'un routage à étapes.

- v452 (PUSH origin **90ce1ec** ; build 23.1.452+452 lancé ; APK→`Downloads/HoPetSit_v23.1.452.apk` + PDF iOS v452) — **W. Refonte bouton central « Paw Map »** (`custom_navigation_bar.dart`) : `_buildCenterMapButton` remplacé par StatefulWidget `_CenterPawMapButton` (SingleTickerProviderStateMixin) = pill arrondi ORANGE (gradient FF6B45→primary, radius 18) plus grand, surélevé, icône `Icons.map_rounded`+`Icons.pets` (pin blanc) + texte « Paw Map », ombre douce, AnimatedScale 0.94 au press, pulse `_pulse` (1.0↔1.05 repeat) + icône 26 quand actif sinon 23. **X. Guidage itinéraire** (`paw_map_screen.dart`) : empreinte animée le long du trajet = `_routeAnimTimer` (Timer.periodic 450ms, idx loop sur `route.points`) → marqueur `route_paw_progress` (hueOrange) unionné PAR-DESSUS `_getMarkersFromCache` (hors cache, pas de réinvalidation) ; `_startRouteAnimation`/`_stopRouteAnimation` ; `HapticFeedback.lightImpact()` au départ (import `flutter/services.dart` ajouté) ; lancé dans `_fetchDirections`, stoppé dans `_clearRoute`+dispose ; **Premium** : ligne discrète `pawmap_premium_spots_nearby` (« 🐾 @n PawSpot à proximité ») dans `_buildDirectionsBanner` si `premiumActive||pawspotActive` + spots chargés. NB : turn-by-turn « Tournez à droite » + modes marche/voiture + trafic = nécessitent un routage avec ÉTAPES (`/pawspots/directions` ne renvoie que polyline+distance) → étape dédiée. i18n `pawmap_premium_spots_nearby` ×6. flutter analyze lib clean + node -c OK + admin JS 3/3.

- v451 (PUSH origin **0cd6fb1** ; build 23.1.451+451 lancé ; APK→`Downloads/HoPetSit_v23.1.451.apk` + PDF iOS v451) — lot bugs. **L. PawSpot sur-censure** : `textModerationService` matchait en SOUS-CHAÎNE (racines « cu »/« pue ») → mots ES innocents masqués ; fix regex ANCRÉE `^(?:…)$` (PROFANITY_RE + EXTRA_RE) = match mot entier. **M. Âge animal « 0m ans »** : (1) PUT /pets ne sauvait pas `age` (absent whitelist `updatePetProfile`) ; (2) sérialisation renvoyait `calculateAge(dob)`=« 0m »/« 2y » + app collait « ans ». Fix backend `serializeAge(pet)` (privilégie `pet.age` années) aux 5 sorties + whitelist `age` ; app `utils/pet_age_format.dart petAgeDisplay()` (localise ans/mois) + 3 sites (pet_profile ×2, my_pets). **N. Ami Premium vu promeneur** : `/friends/family/members` isPremium ne matchait que l'id rôle stocké → premium cross-rôle non vu ; fix détection AUSSI par EMAIL (3 collections rôle → premiumExpiry). **O. Carte agrandie** : `_mapExpanded` Rx ; bannière « En direct » mi-largeur + bouton `pawmap_expand_map` ; mode plein écran = `_buildExpandedOverlayBar` (toggle direct + réduire) ; top controls masqués via Obx. **U. Mode agrandi** : seul le rond Signaler ; si `pawspotActive||premiumActive||followActive` → `_buildTagSpotFab` (rond doré « Tag spot » → `_startSpotPicking`) au-dessus ; zoom pill + around-you + directions masqués en agrandi (Obx dans le child du Positioned, jamais wrapper le Positioned). **S. Zoom suivre/itinéraire** = déjà OK (`_startFollow` zoom 18 + directions fit-bounds). **Q. Suppression compte** : `GET /friends` purge les amitiés orphelines (fini placeholder « Utilisateur supprimé ») ; `/friends/family/members` exclut + `$pull` les membres au compte supprimé ; PawSpots du compte supprimé RESTENT (deleteAccount ne touche PAS PawSpot). **T. Web prestataires sans annonces** : RACINE `getRequestPosts`+`getNearbyRequestPosts` renvoyaient `[]` si sitter sans `canServiceAtOwner`/`canServiceAtSitter` → « non configuré » = « accepte les deux » (filtre lieu appliqué SEULEMENT si ≥1 côté choisi). **P. i18n** : +2 clés (`pet_age_months`, `pawmap_expand_map`) ×6, valeurs ES/PT non traduites corrigées (`pawspot_type_food_cafe`…), ~10 chaînes en dur → `.tr` (View Profile, Publier une promenade, vérif e-mail EN, walker_detail, kyc, add_card, airwallex). flutter analyze lib clean + node -c OK + i18n parité 6 langues.

- v450 (PUSH origin **97069ca** ; build 23.1.450+450 lancé ; APK→`Downloads/HoPetSit_v23.1.450.apk` + PDF iOS `Downloads/HoPetSit_iOS_Build_Guide_v23.1.450.pdf`) — 5 chantiers v450. **A. Thème PAR RÔLE (remplace le jaune v448)** : `AppColors.scaffold(context)`/`inputFill(context)` teintés selon le rôle courant — owner orange pâle (0xFFFFF1EC / fill 0xFFFCE4DC), sitter bleu pâle (0xFFEAF2FD / 0xFFDDEAFB), walker vert pâle (0xFFEAF7EE / 0xFFDCEFE3) ; rôle résolu via `_activeRole()` = `user_role` GetStorage → `user_profile.role` → `activeRoleOverride` (posé par `signup_wizard_screen` build, purgé au logout `auth_controller`) → owner. `lightGrey`/`chatFieldColor` revert gris neutre 0xFFF1F2F4. Yellows en dur v443-448 repassés en pâle de rôle (pet_profile `_paragraph`, edit_pet bg+placeholder via `activeRoleAccent()`, owner_profile_view À propos, pet_post_card en-tête « Ma publication »+À propos owner). main.dart `scaffoldBackgroundColor` = `scaffoldLightForRole()`. **B. Boutons profil pâles** : owner « Modifier mon animal » (profile_screen `_buildQuickActions`) orange pâle + bordure ; sitter calendrier+portefeuille (`_sitterWideAction`) bleu pâle (plus de plein bleu) ; walker déjà vert pâle. **C. Chat « Partager mon numéro » sur 3 profils** (avant sitter seul) : backend `/share-phone` rendu role-agnostic (owner/sitter/walker, lit mobile+countryCode du bon Model, incrémente le bon unread, plus de `requirePaidBooking`) ; `PhoneShareCard` (widgets/phone_share_card.dart — Appeler tel: + Copier) ; getters `isPhoneShare`/`phoneShareNumber` sur ChatMessage+SitterChatMessage ; bouton + confirmation `_onSharePhoneTap` dans owner (AppBar phone pill) + sitter/walker (TextButton) ; i18n `phone_share_*` ×11×6. **D. Boutons email → /open canonique** : `emailLinkBuilder.buildEmailLink` retourne TOUJOURS `${BASE_URL}/open` (ancien switch = `buildDeepLink`, gardé inutilisé) ; app installée → App Link/Universal Link ouvre l'app (deep_link_service `first=='open'||'app'` = no-op gracieux) ; sinon page web `/open` (website/src/app/open/page.tsx : tente `hopetsit://open` puis redirige `/download`) ; AndroidManifest App Links +`/open`+`/app`, AASA +`/open`+`/app` ; i18n web `open_app_*` ×6. **E. PawMap ON/OFF = affichage carte uniquement** : clic ON sans abo → popup `_promptSubscriptionRequired(tab)` « Abonnement requis » / « Voir les offres »+« Plus tard » (remplace snackbar+nav auto) ; infobulle ℹ️ `_showPawMapToggleInfo` + chip au-dessus des 3 switches ; PawPremium pilote déjà PawFollow+PawSpot (v449) ; abo jamais touché par ON/OFF. i18n `pawmap_sub_required_*`/`pawmap_see_offers`/`pawmap_later`/`pawmap_toggle_info*` ×6. flutter analyze lib clean + node -c OK + admin JS 3/3 + website `tsc --noEmit` OK + i18n parité 6 langues (18 clés app/langue). Backend v450 déployé (app.js ADMIN_BUILD + conversationRoutes + emailLinkBuilder). **+ 2e push origin dbe381f (G+H)** : **G. Abo→chat (4 plans)** = RACINE gates chat (chatAccess.js middleware + chatAccessService.js) détectaient l'abo via `currentPeriodEnd`+`hasActivePawFollow` (familyExpiry) mais JAMAIS `pawspotExpiry` → PawSpot SEUL prenait 403 ; fix helper partagé `UserSubscription.hasAnyActiveSubscription(userId)` (DATE sur 4 timers currentPeriodEnd/familyExpiry/pawspotExpiry/premiumExpiry) utilisé dans les 2 gates. **H. PawPoints/promos audit** : badges+niveaux OK ; **promo `percent_discount` JAMAIS appliqué** (validé+redemption mais aucun achat réduit) → fix `subscriptionRoutes` consomme la 1re PromoCodeRedemption percent_discount non consommée pour ce plan + réduit le PI (champ `discountConsumedAt`), app `subscription_controller` utilise le montant réduit renvoyé (`piData['amount']`) ; **récompenses admin custom (PawReward subscription/boost)** débitaient sans octroyer → fix `pawPointsRoutes` octroi immédiat (grantFreePeriod/boostExpiry)+remboursement ; app `pawspot_leaderboard_screen` affiche+échange `cat['rewards']` custom ; web `/pawpoints` affiche custom rewards + champ code promo `checkPromo/redeemPromo` sur boutique. **+ 3e push origin 8931eef (I+J+K)** : **I.** tuile « Choisir un service » retirée des profils owner (`profile_screen` _buildSettingsTile) + sitter (walker ne l'avait pas) — services dans Modifier profil ; inscription↔service OK (wizard collecte selectedServices, OTP route home du rôle, plus de ChooseServiceScreen). **J.** crayon orange retiré de l'avatar fiche animal (`pet_profile_screen` overlay `if(editable) Positioned`) — photo modifiée via Modifier l'animal (pill « Changer la photo » conservé). **K.** suppression compte → retrait amis+map : `userController.deleteAccount` purgeait déjà Friendship+abo propre+doc user mais le compte restait MEMBRE des familles tierces → `$pull familyMembers` de tous les abos tiers + `UserSubscription.listFamilyMembers` exclut désormais les co-membres dont le doc n'existe plus (nettoie orphelins d'avant). Build v450 final (A-K) relancé bgib6216z.

- v449 (PUSH origin **bebcb65** ; build 23.1.449+449 lancé ; APK→`Downloads/HoPetSit_v23.1.449.apk` + PDF iOS `Downloads/HoPetSit_iOS_Build_Guide_v23.1.449.pdf`) — gros lot multi-séances. **App** : badge vérifié VERT+plus gros, gaté `identityVerified` (KYC 3€) — `verified_badge.dart` + icônes vertes (service_provider_card, sitter_bottom_sheet) ; **Modifier annonce = form complet AVEC photos** (nouvel endpoint `POST /posts/:id/media` = `addPostMedia` + multer route + repo `addPostMedia` + section photos ré-affichée en édition) ; inscriptions lisibles (en-tête replié si clavier, `signup_wizard_screen`) ; photo profil sync (owner+walker edit → `ProfileController.loadMyProfile`) ; poids/taille animal vue prestataire (`PostPet` + 4 maps postController) ; temps de réponse OAuth Google/Apple (`authController` sitterFields/walkerFields) ; flèche « N photo » retirée + heure annonce NOIR (`pet_post_card`) ; réservation Détails null-safe (`booking_agreement_screen`) + annuler-72h backend accepte `paymentStatus==paid` ; **PawMap** : `_togglePawPremium` pilote `_showLiveLayer`+`_showPawSpots`, halos POI retirés, fallback marqueur ami `_paintFallback` 🐕+blanc, **viseur signalement** `_pickingReportPos`+`_buildReportPickerOverlay` (clés `pawmap_report_pick_*` ×6) ; **notif 72h** `processServiceStartT72hReminders` (payoutScheduler) + Booking.startServiceT72hReminderSentAt + template `service_start_t72h` ×6 ; login Google/Apple annulation silencieuse ([[messaging-notifications-audit]]) ; thème jaune clair global (`AppColors.scaffoldLight`/`inputFillLight`). **ADMIN** : i18n COMPLET FR/EN/ES (helper `t()` + `currentAdminLang`/`adminLocale`/`refreshActivePageI18n`, ~399 clés/langue, re-render dynamique au switch, anglais résiduel normalisé). **SITE WEB** : paiement réservation (Airwallex/PayPal + `/pay/done` confirm) + calendrier dispo prestataire (`getMyAvailability`/`updateMyAvailability` GET/PUT `/sitters|walkers/me/availability`, **pluriel**) + gate dates indispo. flutter analyze lib clean + node -c OK + admin JS 3 blocs OK + website `tsc --noEmit` OK. NB site : booking-pay/calendar étaient « in app » par design avant v449.

- v448 (BUILDÉE 23.1.448+448 + PUSH origin **6e3e0cb** ; APK 95M `Downloads/HoPetSit_v23.1.448.apk` + AAB 67,2M + PDF iOS `Downloads/HoPetSit_iOS_Build_Guide_v23.1.448.pdf`) — gros lot multi-séances. **Bugs fiche animal/profils** : vaccination « cannot be empty » débloquée (backend validation retirée + frontend conditionnel) ; stylo photo animal pose l'avatar même sans publicId + recharge la fiche ; suppression animal ne bloque que sur résa active non terminée ; fond « Modifier l'animal » jaune ; boutons walker vert pâle ; pop-ups edit animal `.tr` ; boutons « Passer en… » côte à côte ×3 profils ; heure annonce = case grille (`post_field_time` ×6) + en-tête « Ma publication » jaune ; bouton « Modifier l'animal » en haut de la fiche (FAB retiré). **PawMap** : police cartes Mon cercle/Mes signaux réduite ; switches abo on/off manuels (PawFollow→`_showLiveLayer`, PawSpot→`_showPawSpots`, PawPremium→`_showPremiumLayer`) ; **fix pins premium invisibles = `mapReportRoutes.resolvePremium` reconnaît le STAFF**. **AUDIT messagerie/notifications complet** cf [[messaging-notifications-audit]] : badge double-comptage sitter/walker, badge force-0 onglet chat, `syncChatBadgeFromServer` dédup, cloche sur-compte, bandeau `_isResponding` + gardes anti-zombie, **email gating hors-ligne** (`isUserOnline`), canal FCM Android, webhook `piId` ReferenceError + `wasAlreadyPaid`, `new_request_nearby` self-notify, script `backend/scripts/audit_messaging_cleanup.js`. **Login** : annulation Google/Apple (code 16 canceled) n'affiche plus d'erreur. **Thème** : tous fonds gris clair → jaune doux app-wide (`AppColors` scaffoldLight/inputFillLight/lightGrey/chatFieldColor). flutter analyze lib clean + node -c OK + i18n parité 6 langues. RESTE : ESP emailing (délai Gmail SMTP), tests réels 2 téléphones, vérif Info.plist iOS.

- [Carte blanche app](maps-blank-google-cloud-key.md) — **2 causes** : fond uni qui transparaît + site web OK = RENDU Android TLHC → fix v454 `useAndroidViewSurface=true` (VRAIE cause chez Daniel) ; quadrillage gris = clé Google Cloud (SHA-1 release `64:1E:…:ED:85` + `com.hopetsit.app`)
- [Audit messagerie/notifications](messaging-notifications-audit.md) — v448 audit complet badges/push/email/cloche/bandeau : vraies causes (2 écrivains badge legacy, email sans gating hors-ligne, webhook piId ReferenceError) + ce qui reste ; stack = Socket.IO+FCM+Mongo (PAS Supabase/SQL)

- v446 (BUILDÉE 23.1.446+446 + PUSH origin **8c26163** ; APK `Downloads/HoPetSit_v23.1.446.apk`) — peaufinage fiche animal (retours Daniel sur v445 « gros progrès ») : FAB fiche `PetProfileScreen` label `post_action_edit`→**`pet_edit_animal`** « Modifier l'animal » (+ titre `EditPetScreen._buildTitle` idem, plus « l'annonce ») ; **marge basse 96.h** sur les 4 onglets (`EdgeInsets.all(16.w)`→`fromLTRB(...,96.h)`) → le FAB ne masque plus Documents/Galerie ; **photo avatar cliquable** (GestureDetector→`pickPetProfileImage`) en + du stylo sur `edit_pet_screen` ; **Traitement en cours = `_yesNoRow` Oui/Non** (pastille) dans l'onglet Santé fiche + détail si présent ; `_paragraph` non-danger gris→**jaune 0xFFFFF8E1** (« fond gris → jaune »). i18n `pet_edit_animal` ×6. flutter analyze clean. Backend v446 = juste app.js.

- v445 (BUILDÉE 23.1.445+445 + PUSH origin **0323f6f** ; APK `Downloads/HoPetSit_v23.1.445.apk`) — lot bugs Daniel en deep. **CAUSE RACINE cadre gris géant 3 profils « Modifier le profil »** cf [[profile-choice-chips-obx-graybox]] = `Obx(() => ProfileChoiceChips(selected: controller.X))` passait la RxList PAR RÉFÉRENCE ; le `.contains()` se fait dans le `build()` de l'ENFANT → hors scope tracking de l'Obx → Obx ne lit aucun observable → **GetX throw → ErrorWidget GRIS en release**. (Mon fix contraste v444 inputFill→card était un FAUX diagnostic.) Fix = `.toList()` dans l'arg (×7 : owner searchServices, sitter+walker services/animals/experience) → l'Obx lit la RxList. Le sélecteur « Langue » marchait car son Obx lit `.contains` DANS le builder (asymétrie = preuve). **#1** services prestataire affichés côté owner (`service_provider_detail_screen` sitter + `walker_detail_screen`) + `.tr` manquant `sitter_detail_controller:47`. **#2** inscription : « Assurance PawMap » retirée (`signup_wizard` owner prefs). **#3** libellés expérience « Ancien promeneur/pet-sitter » → « Promeneur/Pet-sitter professionnel » (×6). **#4** badges du cadre coloré (`active_benefits_row`) grille 2-col → ligne HORIZONTALE scrollable. **#6** owner « À propos de moi » = chaîne backend VÉRIFIÉE 100% correcte (updateProfile→Owner.findByIdAndUpdate $set bio ; getOwnerProfile→sanitizeUser=sanitizeDoc passthrough inclut bio ; feed `.populate('ownerId')` doc complet) → aucun bug code, retest build. **#7** création animal avatar gris→jaune doux (0xFFFFF3C4 + patte 0xFFE8A00A). Walker « Type de service » (Promenade indiv/collective/grande balade/visite) éditable dans Modifier profil, persisté `Walker.service` (PATCH whitelist déjà OK). « Traitement en cours » Oui/Non (`treatmentOngoing` RxString, dérivé du texte) éditable. **Fiche animal 100% modifiable APRÈS save VÉRIFIÉ** : round-trip symétrique populateFrom↔toPayload, getPetById renvoie TOUT, ENRICHED_PASSTHROUGH persiste TOUT, getMyPets=sanitizePet passthrough, FAB Modifier editable:true ; + garantie `_refreshFullPetSilently` (refetch getPetById complet à l'ouverture édition même si pet passé en arg). Tarifs walker `my_rates_screen` 30/60/90/120 → **30min/1h/2h** (90 retiré, 60→1h/120→2h ×6) + phrase `walker_currency_info` qui débordait (Row sans Expanded → Expanded+maxLines). Inscription walker active (signup_wizard) déjà 30min/1h/2h + animal sup + temps réponse (sign_up_screen.dart = mort, non routé). Badge messages déjà fixé v444 [[chat-badge-1-then-5-fix]]. flutter analyze clean + i18n parité 6 langues 0 doublon + node -c OK. Backend v445 = juste app.js (authController inscription + postController getPostById déjà live depuis v444 9eaa8ff).

- v444 (BUILDÉE 23.1.444+444 + PUSH origin **9eaa8ff** ; APK 95M `Downloads/HoPetSit_v23.1.444.apk` + AAB 67,2M) — lot bugs Daniel en deep (1 par 1, après interruption « ne rebuild pas ») : **#1 Ajouter animal** = `edit_pet_screen.dart` réécrit, plus de doublon sections inline + `PetExtraFields` → garde QUE la photo + UN ExpansionTile « Informations principales » (Nom/Race/Catégorie/Naissance/Poids/Taille/Couleur/Passeport/Puce/Médicaments/Bio) puis À propos/Santé/Habitudes/Documents (une seule « Santé ») ; classe `_ModernSectionHeader` supprimée ; tout reste envoyé par `validateAndUpdateProfile`. **#2 Fiche animal éditable** = `PetProfileScreen` FAB « Modifier » visible sur TOUS les onglets → `EditPetScreen`. **#3+#5 cadre gris « Ce que vous recherchez »** = CAUSE RACINE `ProfileChoiceChips` fond `inputFill`==`scaffold` (0xFFF7F7F8) → chips invisibles ; fix → fond `card`(blanc)+bordure 1.2 (vaut owner+sitter+walker). **#4 Wallet près IBAN** = actions rapides walker+sitter réordonnées `📅·🛒·🏦 IBAN·💼 Wallet`. **#6 profil owner depuis annonce** = chaîne déjà OK (backend feed+getPostById `owner.bio`+`pets`, PostModel parse, 2 appelants passent, écran affiche) — le trou getPostById nu était v443, corrigé. **#7 inscription** = `authController.signup()` droppait country/dateOfBirth/bio owner/experienceTags/acceptedPetTypes/availableDays/searchPreferences/preferences → ajoutés aux 3 payloads `Model.create` (additif). **#8 BADGE messages « 1 puis 5 qui revient »** cf [[chat-badge-1-then-5-fix]] = (a) `_onSocketMessageNew` SANS dédup × émission 3 rooms de rôle + `chat_controller` ré-incrémentait = sur-comptage ; (b) `clearChatBadge()` tap onglet forçait 0 local alors que serveur gardait les non-lus → vrai total « revenait ». Fix source unique=serveur : dédup par id de message (`_seenChatMsgIds`) + `scheduleChatBadgeResync()` débouncé (après bump ET après lecture conv) + tap Chat = `syncChatBadgeFromServer()` (vrai total) au lieu de 0 ; backend déjà correct (compteur par rôle owner/sitterUnreadCount + push multi-profils `gatherFcmTokens` union 3 docs + mail). **#9 site** PawMap bouton Télécharger `/boutique`→`/download`. AUSSI dans ce push (préparé avant interruption) : boutique chat-entre-amis retiré + promo % affichée (display-only) + signup tarifs walker 30/1h/2h + Temps de réponse (#119/#120) ; flux DEMANDE pets cliquables + envoyer-demande sans « infos manquantes » (#123) ; badges abo discrets jours-seuls pastille couleur-abo (#124) ; site web sync réservations mission-finie/notation + rendre/recevoir animal (#125, `ServiceConfirmation.tsx`). flutter analyze clean + node -c OK + npm build OK. i18n parité 6 langues (seules clés `stripe_*` mortes manquent — non utilisées).
- v443 (BUILDÉE 23.1.443+443 + PUSH origin f6fa34b ; APK 95M `Downloads/HoPetSit_v23.1.443.apk` + AAB 67M) — retours Daniel : **FIX paiement** (commission 20% payée par l'OWNER, prestataire reçoit 100% ; `_buildProviderGainBlock` affichait brut×0,9 inversé — corrigé via `est.net`/`est.brut`, cf [[provider-annonce-gain-sync]]) ; **annonce** heure→horloge sous Service + Service bleu(sitting)/vert(promenade) + cadre owner JAUNE (0xFFFFF8E1) + tri « plus récent » retiré (home owner) ; **fiche animal** sections dépliées (initiallyExpanded) + **habitudes 14 cochables** (`habits.tags`/habitPresets) + **documents 6 cochables** (`documentTypes`/documentPresets, upload→galerie) + **galerie auto-refresh** (`PetProfileScreen._reopenFresh` Get.off pet frais au retour) ; Pet.js +habits.tags +documentTypes (GET hand-built + ENRICHED_PASSTHROUGH) ; i18n +23 clés ×6 (pet_habit_*, pet_doc_*). flutter analyze clean. AUSSI v443 (API rétablie, agents OK) : cartes recherche owner pixel-perfect + clic→profil prestataire (#102) ; onglet Tarifs OK (#101) ; PawSpot récompenses ouvertes sans badges (#99) ; anciens filtres home walker/sitter retirés (#100) ; **favoris prestataires** end-to-end (`Owner.favoriteProviders` + POST/DELETE/GET `/users/me/favorites` + `FavoritesController` optimiste + cœur cartes) ; **logo app** (`assets/brand/png/ic_launcher.png`) à côté du rôle sur les 3 profils (remplace pictogramme) ; **logo PawBoost** onglet boutique = fusée (pas d'asset dédié) ; **« 20 signalements premium utilisables »** ajouté aux 4 abos (PawFollow/PawFamily/PawSpot/PawPremium, clé `shop_premium_reports_included`) ; **profil owner consultable depuis l'annonce** (prestataire) via `onOwnerTap` (pet_post_card) → nouveau `views/service_provider/owner_profile_view_screen.dart` (avatar/nom/ville/À propos/animaux→fiche) ; owner→prestataire déjà OK (cartes). Backend additif total (Pet.js, petController, Owner.js, userController, userRoutes, sitterController) DÉPLOYÉ. 2e vague v443 : **PawMap redesign** (paw_map_screen : pas de scroll horizontal — filtres 4 boutons Expanded + légende Wrap ; vrai logo PawSpot = `GoldenPawCoin` ; PawFollow|PawSpot|PawPremium 3 colonnes ; écran Signaler `create_report_sheet` header fixe+footer Publier toujours visible) ; **fix BADGE messages sitter/walker** = root cause backend `sockets/emitter.js` `emitChatMessage` recevait une conversation SANITIZÉE (ownerId/sitterId/walkerId supprimés) sur le chemin socket → destinataires vides → `message:new` jamais émis → badge jamais incrémenté (fix : tolère les 3 formes) ; **favoris prestataires** = `Owner.favoriteProviders` + endpoints + `FavoritesController` + cœur cartes ; **logo app** badge rôle ×3 + **logo PawBoost** onglet = fusée ; **profil owner depuis annonce** (`onOwnerTap` → `owner_profile_view_screen`) ; **« 20 signalements premium »** ×4 abos ; **site web** i18n PawMap/PawPoints + logo PawMap = SVG identique app (gap : anciennes sections /boutique encore FR en dur) ; **admin** liste TOUTES récompenses (custom éditables + 6 abos intégrés). cf [[provider-annonce-gain-sync]]
- v442 (23.1.442+442, BUILDÉE + PUSH origin commit 8d0aea5 ; APK 94,6M `Downloads/HoPetSit_v23.1.442.apk` + AAB 67,3M) — gros lot UI : toggles `AppSwitch` [[app-switch-toggle-readability]] ; thème Clair/Sombre/Système + langue app + cloche profils + préremplissage région inscription [[theme-system-and-locale]] ; détail annonce prestataire vert/bleu + gain synchronisé [[provider-annonce-gain-sync]]. Aussi : Bio→« À propos de moi » ×3 profils + sync inscription, cadre gris animaux acceptés → sélecteur icônes, cartes recherche owner (extraPetRate + icônes animaux + « Indisponible » si calendrier vide + filtre « cette semaine »), annonce owner « Modifier » → édition complète (backend 409 `POST_PAID_LOCKED` si payé), teintes annonce (caractère jaune pâle / owner orange pâle), barre « Autour de moi »+rayon km sur accueils walker/sitter (`views/shared/widgets/around_me_search_bar.dart`). flutter analyze clean + node -c OK. Backend additif (postController/petController/Pet.js/sitterController) ⇒ push origin requis au build.

- [pet enriched fields persistence](pet-enriched-fields-persistence-gotcha.md) — v440 : updatePetProfile (PUT /pets/:id) whitelistait + getPetById hand-built DROPPAIENT les champs fiche enrichis (round-trip cassé) ; fix passthrough + sérialisation complète ; nouveaux champs sterilized/microchipped/foodRestrictions/withNac/sleep/housetrained/leashBehaviour/fears ; annonce horaire = PostDateLabel ; ~37 clés i18n pet_*
- [git deploy remote gotcha](git-deploy-remote-gotcha.md) — deploys = push to `origin` (hopetsit/hopetsit), NOT `dadaciao`; uncommitted local work means Render keeps serving old code; verify via /__build

- [getSitterProfile manual response gotcha](getsitterprofile-manual-response-gotcha.md) — GET /sitters/:id is hand-built (not sanitizeUser); new Sitter fields must be added there or they vanish from the API
- [bande vs cloche vocabulary](bande-vs-cloche-vocabulary.md) — "la bande" = HomeQuickActionBar orange banner, NOT the 🔔 bell feed; one shared widget covers all 3 home screens
- [python3 Store-alias hook error](python3-store-alias-hook-error.md) — the per-edit check-sql-files.py error = sandboxed python3 Store alias; fixed via PATH, clears after app restart; CockroachDB plugin is irrelevant
- [payment escrow mechanism](payment-escrow-mechanism.md) — withdrawable=false = blocked; processProviderPayoutForBooking is the single release chokepoint; gate it on confirmation, never release at payment
- [withdrawal pipeline tooling](withdrawal-pipeline-tooling.md) — retrait bloqué ? bouton ▶️ Traiter maintenant (trace pas-à-pas + lastSchedulerRunAt); Airwallex: id bénéficiaire pas à la racine + postcode UE obligatoire; erreurs horodatées, sans date = fossile
- [PawSpot = communautaire depuis v353](pawspot-community-redesign.md) — fini les halos boost ; spots+points+badges+abo 4,99/39,99+essai 7j ; pawspotExpiry indépendant ; accent doré E8A00A
- [Préparation fiche Google Play](google-play-listing-prep.md) — prochaine session : textes 6 langues + bannière + questionnaires ; v393 = version finale (Premium 7,99/59,99, PawBoost 3,99→19,99, 1 compte=3 profils) ; vérifier suppression de compte + privacy URL d'abord
- [Badge chat : 3 sources](chat-badge-three-sources.md) — unreadChat = socket message:new + push FCM foreground + resync au resume ; v401 fix multi-profils (emit aux 3 rooms de rôle, deploy-only)
- [Parité site + codes promo v402/v403](site-parity-and-promo-v402.md) — site fait tout comme l'app (vérif email, switch rôle, annonces, messagerie) sans rebuild ; système promo (admin prêt, redemption app à builder + onglet « Offre » profil après la vidéo) ; favicon raster (Google ignore SVG)
- [FCM tokens fragmentés multi-profils](fcm-multiprofile-token-fragmentation.md) — push absent mais email OK = token enregistré sous un autre rôle ; fix v408 gatherFcmTokens (union 3 docs) ; demande de suivi badge = $inc unreadCount manquant
- [Refonte app v413 buildée](refonte-app-v413-build.md) — grande refonte maquettes buildée en 23.1.413+413 (fiche animal/profils/inscriptions wizard/gain estimé) ; backend live v412 ; LOT WEB restant : chat suivi animal + photos + PawFamily 5 membres
- [iOS push : aps-environment + clé APNs](ios-push-aps-environment.md) — pas de push iPhone (Android OK) = entitlement aps-environment manquant (fixé v413) + clé APNs .p8 à uploader dans Firebase Console (Daniel)
- [Récompenses PawPoints éditables](pawpoints-rewards-system.md) — PawReward + admin CRUD ; app+site lisent /pawpoints/catalog → modif admin live sans rebuild ; page web /pawpoints + sheet app
- [Suivi direct en arrière-plan](live-tracking-background-gotcha.md) — PawMap dispose() coupait le broadcast (retiré) ; foreground service Android + AppleSettings couvrent le background, PAS le swipe-kill
- [Inscription promeneur plantait](walker-signup-acceptedpettypes-crash.md) — Walker.acceptedPetTypes enum strict ≠ clés wizard (dog/cat/small/nac) ; enum retiré (deploy-only) ; + langues 3→6 + aperçu enrichi (v418)
- [Sauvegarde profil web cassée](web-profile-save-route-gotcha.md) — PUT /users/me/profile tombait sur /:id/profile (id='me' → 400) ; route PUT+GET /me/profile role-agnostic ajoutée v414 ; préférences+2FA sur /profile
- v417 buildée (23.1.417+417, APK 95M `Downloads/HoPetSit_v23.1.417.apk` + AAB 67M, PDF iOS `Downloads/HoPetSit_iOS_Build_Guide_v23.1.417.pdf`) : **refonte PawPoints** (7 niveaux + 2 compteurs + 6 récompenses abo auto-appliquées 1×/user) cf [[pawpoints-rewards-system]] ; admin pages de droite traduites FR. (v416 = suivi direct survit au swipe-kill Android via flutter_background_service cf [[live-tracking-background-gotcha]] ; PawMap cercle violet + logo + 4 signalements ; admin auto-reload si périmé)
- [Profil persistant figé = photo grise map + onglets périmés](getstorage-userprofile-stale-gotcha.md) — GetStorage userProfile écrit qu'au login ; fix v424 `_persistFreshProfile` ; durcissement FriendMarkerService (UA + retry)
- v424 (23.1.424+424) — refonte 6 pages maquettes : fiche animal claire (bannière+avatar+« À jour »+4 onglets), détail annonce enrichi (bande animaux+nb photos, « Caractère des animaux », Dates/Localisation/Nombre/Type/Service + « En attente », « À propos de moi » owner), publier-demande (aperçu chips caractère), recherche promeneurs vert/sitters bleu (déjà OK) ; backend additif postController : owner.bio + pet.category + pet.characterTraits dans la sérialisation des 3 endpoints ⇒ **deploy backend requis** (git push origin)
- [Login multi-rôles + edit animal](multirole-login-precedence-gotcha.md) — email multi-rôles se connecte owner>sitter>walker (ordre fixe) ; fix v425 login `role` préféré ; + hauteur animal rendue optionnelle à l'édition
- v426 (23.1.426+426, BUILDÉE + PUSH origin fait commit 8337daa) — carte annonce premium `PetPostCard` partagée : grande photo bannière + badge N photos, animaux en lignes « Nom • N photos ›», accent COULEUR DU RÔLE via `viewerRole` (owner orange 33→221 / walker vert 223 / sitter bleu 224), carte Caractère + À propos owner. + publier-demande maquette 222 (cartes animaux premium + caractère groupé par animal). Fixes : « créé promeneur → ouvre sitter » (login rôle préféré, 4 couches), « Mes animaux pas modifier » (hauteur optionnelle à l'édition), photo grise map + profil pas à jour ([[getstorage-userprofile-stale-gotcha]]). Backend postController (owner.bio+pet.category+characterTraits) + authController.login (rôle préféré) **déployés** (push origin). i18n +26 clés ×6.
- v427 (23.1.427+427, push 9558e03) — cartes recherche owner premium : `SitterCard`/`WalkerCard` (onglets Pet-sitters bleu / Promeneurs vert) → stat « gardes/promenades réalisées » (completedServicesCount/completedWalksCount), « À propos de moi » encadré, CTA renommé **« Demander à cette personne »** (l'owner demande direct au prestataire). Frontend only. RESTE à faire : vue walker/sitter d'une annonce (maquettes 223/224 « Postuler à cette demande ») = refonte complète `PetPostCard` provider (header owner note+distance+publié, cartes animaux âge/sexe, grille Dates/Lieu/Animaux/Service, « Caractère de vos animaux » coches vertes, « Informations importantes », « Votre gain estimé » détaillé, Sauvegarder/Partager) — partiellement fait en v426 (bannière+couleur rôle+lignes animaux+caractère+à propos+gain).
- v428 (23.1.428+428, push c1ec7c2) — « Mes animaux » maquette 42 : `MyPetsScreen._buildPetCardV2` carte propre (photo gauche + nom/race + badge À jour + métriques âge/poids/taille + puces espèce/sexe + chevron, tap→fiche) ; ancien `_buildPetCard` gardé en `// ignore: unused_element`. `PetProfileScreen` : param `onDelete` + menu ••• Modifier/Supprimer. RESTE (task #75) : `CreatePetProfileScreen` (861 lignes) refonte premium sections + options fiche (maquette 40→43).
- v429 (23.1.429+429, push 7a9467f) — détail annonce fidèle maquette 221 : `PetPostCard` « Demande de réservation » en GRILLE 3 col (cases pastille icône+libellé+valeur Dates/Localisation/Nombre/Type/Service/Détails via `_buildReservationGrid`+`_resCell`), carte « Caractère des animaux » PAR animal (`_petCharacterBlock` : icône espèce+nom noir+race+description) gated par `_hasCharacterData`, `_speciesEmoji` gère le FR (chien→🐕/chat→🐈), nom animal noir + compteur photos couleur rôle. Backend postController pet.breed+pet.bio (additif 3 endpoints) + PostPet model breed/bio. ⚠️ carte Caractère visible SEULEMENT si l'animal a bio/characterTraits renseignés. RESTE 221 100% : en-tête owner (boutons Modifier/Supprimer étiquetés + localisation + note). i18n +post_field_details ×6.
- v430 (23.1.430+430, push b523bde) — cartes recherche owner REMPLACÉES par composant premium maquettes 50/51 : `SitterCard`/`WalkerCard` recréées (ancien `_ratePill` supprimé) = avatar+pastille en ligne, nom+vérifié+puce rôle, note+(N avis), ville+distance, colonne stats (gardes/promenades+dispo+réponse), tarifs en cartes (jour/sem/mois | 30/60/120min + animal suppl.), À propos, CTA bleu « Envoyer une demande » / vert « Demander une promenade ». Modèles Sitter/Walker +extraPetRate +responseTimeMinutes (rendu conditionnel, vides tant que backend ne les envoie pas).
- v431 (23.1.431+431, push 2bdf11b) — **MIGRATION COMPLÈTE profil animal, zéro coexistence** : SUPPRIMÉ `create_pet_profile_screen.dart` + `create_pet_profile_controller.dart` + ancienne `_buildPetCard`. CRÉÉ `pet_gallery_screen.dart` (PetGalleryScreen : photos/vidéos add/delete/principale) + `utils/pet_species_color.dart` (chien orange/chat bleu/reptile jaune/oiseau vert/rongeur rose/cheval violet). `EditPetScreen`+`EditPetController` = écran UNIFIÉ create+edit (petId vide→POST createPetProfile, sinon PUT updatePet ; `isCreate` getter). `PetProfileScreen` : accent par espèce, « Changer la photo »/« Ajouter des photos »→PetGalleryScreen, ••• Modifier→EditPetScreen. Rerouté Mes animaux ×2 + onboarding (choose_service) → écran unifié create. **grep `CreatePetProfile*` = 0 réf**. ⚠️ « définir photo principale » = upload nouvelle image (pas de promotion d'URL stockée, backend ne le permet pas).
- v432 (23.1.432+432, push ebed633) — accueil owner en-tête recherche refait maquettes 50/51 : SUPPRIMÉ ancien slider rouge `_buildInlineDistanceSlider`/`_distanceSliderBody` + `CustomSegmentedControl`. CRÉÉ dans `home_screen.dart` : tabs pastilles (icône+libellé, fond bleu sitters/vert promeneurs/orange mes-pubs), bloc « 📍 Autour de moi / ville » + « Rayon » slider 10→500 km coloré, ligne filtres (Animaux gardés/Promenades · Disponibilité · Trier), compteur « 🎯 N pet-sitters/promeneurs autour de moi · N résultats trouvés ». « Trier » = tri client distance/note ; les 2 autres = sheets « Bientôt ». Composer Publication visible UNIQUEMENT sur Mes publications. Cartes SitterCard/WalkerCard (v430) intactes. i18n +13. RESTE non fait (demande précédente non répétée) : publier-demande cartes animaux nb-photos+badges inline + section « Lieu de garde » (Chez moi/sitter/both existe déjà mais conditionnelle) affichée dans l'annonce.
- v433 (23.1.433+433, push af92343) — fix « Changer la photo de profil animal ouvrait la galerie » : crayon avatar + bouton « Changer la photo » → `_changeProfilePhoto` (pick→uploadPetMedia avatar→refresh), onglet Galerie garde PetGalleryScreen. Traits caractère déjà OK (12 presets EnrichedPetFormState).
- v434 (23.1.434+434, push 6fae287) — fix SIGNALEMENTS PawMap : **premium gate report** lisait `SubscriptionController.isPremium` (false) au lieu de reconnaître `PawSpotController.premiumActive` (true). Nouveau `utils/report_premium_helper.dart` `ReportPremiumHelper.isUnlocked = isPremium OU premiumActive` → PawPremium débloque les signalements premium. Doublon emoji corrigé (aggressiveDog+strayPet = 🐕 → strayPet 🐾, map_report_model). Bouton « Signaler » (FAB paw_map `_buildReportFab`) ouvrait l'ANCIENNE `ReportCategoryGridScreen` → ouvre maintenant `CreateReportSheet.show(initialPoint)`. ReportCategoryGridScreen plus référencée par code vivant. ⚠️ Demande globale 9 chantiers (profil↔inscription, tarifs+dispos sitter/walker, PawPoints/Pawspot/PawFollow/PremiumStaff) : reste à faire 1-par-1 ; beaucoup existent déjà depuis sessions antérieures (installer le dernier APK d'abord).
- v435 (23.1.435+435, push 4f04d9d) — 3 gros chantiers via agents parallèles. **#2 tarifs+dispos** : sitter daily/weekly/monthly + walker 30/60/90/120 + extraPetRate + responseTimeMinutes éditables/persistés/renvoyés (rates+availability+public+nearby) ; calendrier dispo rendu role-aware (walker était en 403 silencieux) ; schémas Sitter.extraPetRate+availableTimeSlots, Walker.extraPetRate. **#4-7 premium** : `/users/me/benefits` force premium/pawfollow/pawspot/isPremium=true pour STAFF (clé de propagation partout) ; Pawspot récompenses cosmétiques débitent `pawPointsSpendable` (plus le lifetime) ; PawFollow gaps staff (live+famille) corrigés ; `post_purchase_refresh` rafraîchit PawSpotController. **#1+#3 profil↔inscription** : DOB + animaux acceptés/promenés + expérience + services + rayon synchronisés owner/sitter/walker ; doublons retirés (skills owner, servicePreferences legacy→searchPreferences). Backend additif (déploy requis). #8 signalements = v434. #9 QC = vérif statique (pas de device). flutter analyze clean + node -c OK sur tous les fichiers backend.
- v436 (23.1.436+436, push 43f6524) — 8 retours v435 : grille « Demande de réservation » gris→orange pâle (accent rôle) ; **fix « me sort les 3 animaux »** = postController `resolvePostPets` renvoie SEULEMENT `Pet.find({_id:$in post.petIds})` (fallback tous si vieux post) au lieu de tous les animaux du owner ; « Notes supplémentaires »→« Détails » ; Lieu de garde +3e option « Les deux » + `serviceLocation` envoyé/persisté/affiché (cellule grille PetPostCard, 4 call sites) ; filtres accueil branchés (Animaux gardés/type + Disponibilité aujourd'hui/semaine filtrent vraiment) ; « Autour de moi » changement de ville (HomeController searchCity/Lat/Lng + CityLocationPicker) ; boutique Pawspot connectée au vrai catalogue PawPoints (coin_shop → `showPawPointsRewardsSheet`, fini les récompenses legacy hardcodées). i18n +clés ×6.
